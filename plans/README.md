# TRR Workspace — Implementation Plans

Generated by `/improve-more` (deep audit) on **2026-07-06** against:
workspace-root `fb76b5b`, TRR-Backend `8ea7aa1a`, TRR-APP `83778e5c`.
Recon facts and verified commands live in [recon.md](recon.md).

Reconciled on **2026-07-07** against the same committed heads plus the live dirty
worktrees. The existing six plans remain current; no new plan was written because
the only newly noticed local artifact (`TRR-Backend/.github/.DS_Store`) is
ignored and untracked, not repository debt.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, update your row
when done.

**Plans 001–006 were hardened on 2026-07-07** via a `review-plan` pass (one
cold-context reviewer per plan cross-checking every excerpt and command against
the live repo). Fixes applied: corrected plan 001's fix approach (the original
per-test `os.environ` snapshot could not remove import-time pollution — now uses
a `tests/conftest.py` pristine baseline + surgical restore); corrected the
`.venv/bin/ruff` → `ruff` command across 001/002/004 (no venv ruff exists);
corrected plan 004's false "`_mark_run_finalize_pending` does not exist" claim
(it exists at `social_season_analytics_impl.py:14047`) and closed a test-harness
gap; made plan 003's traversal tests actually discriminate the guard; and pinned
plans 005/006 against unverifiable branches. No plan changed scope.

---

## ⚠️ Read before committing these plans (repo visibility)

**All three repos are PUBLIC** (`therealityreport/trr-backend`, `/trr-app`,
`/trr-workspace`). This `plans/` directory lives in the public `trr-workspace`
repo and is **not** git-ignored — committing it publishes these plans, including
the two that describe security-hardening work (002 SQL identifier hardening, 003
Getty path traversal). The plan files deliberately contain **no** secret values
and **no** runnable exploit strings (only code patterns + remediation), so the
exposure is bounded — but before you `git add`/push, decide one of:

1. Add `/plans/` to the workspace-root `.gitignore` and keep these local, or
2. Review 002 and 003 and confirm you are comfortable with the vulnerability
   *descriptions* being public before the fixes land (they are already
   remediation-focused), or
3. Move the security plans somewhere version-control-ignored and keep the rest.

No secrets were reproduced anywhere in these files. `keys/` (GCP service-account
JSON) is git-ignored and untracked — verified during the audit.

---

## Execution order & status

| Plan | Title | Repo | Priority | Effort | Risk | Depends on | Status |
|------|-------|------|----------|--------|------|------------|--------|
| [001](001-backend-test-env-pollution.md) | Isolate `os.environ` mutation in `test_modal_jobs.py` (unblocks full test lane) | Backend | P1 | M | MED | — | DONE locally 2026-07-07 — added pristine env restoration around Modal runtime defaults; `tests/test_modal_jobs.py` is green in isolation and in the combined target run |
| [002](002-sql-column-identifier-hardening.md) | Validate dynamic SQL column identifiers in screentime `update_*` | Backend | P1 | S | LOW | — | DONE — executed+approved 2026-07-07, commit `902a69ad` on `advisor/002-sql-column-identifier-hardening` (awaiting user merge) |
| [003](003-getty-prefetch-token-path-traversal.md) | Validate Getty prefetch tokens as UUIDs (path traversal) | App | P1 | S | LOW | — | DONE — executed+approved 2026-07-07, commit `343c2a96` on `advisor/003-getty-prefetch-token-path-traversal` (awaiting user merge) |
| [004](004-run-lifecycle-counter-characterization-tests.md) | Characterization tests for run-lifecycle counters (safety net) | Backend | P2 | M | LOW | 001 | DONE locally 2026-07-07 — dependency satisfied by 001; run-lifecycle counter characterization tests pass with `tests/test_modal_jobs.py` and `tests/api` |
| [005](005-social-progress-lane-counts.md) | Fix misleading comments/media progress lanes | App | P2 | S+ | LOW | — | DONE — executed+approved 2026-07-07, commit `8871b3e5` on `advisor/005-social-progress-lane-counts` (awaiting user merge) |
| [006](006-wire-playwright-e2e-into-ci.md) | Run Playwright e2e smoke in CI (installed, never run) | App | P2 | S | LOW | — | DONE locally 2026-07-07 — non-live Playwright baseline passes with CI-style placeholder env; CI now runs browser setup and e2e smoke as non-blocking full-lane signals |
| [007](007-async-cast-screentime-video-import.md) | Queue cast-screentime remote video imports instead of blocking the admin request | Backend + App | P2 | M | MED | — | DONE locally 2026-07-07 — queued import/status polling implemented; focused backend/app tests pass; Modal deploy pending |
| [008](008-parallelize-social-landing-payload.md) | Parallelize independent social landing payload phases | App | P2 | M | LOW | — | DONE locally 2026-07-07 — social progress now starts before cast SocialBlade resolves; focused app tests pass |
| [009](009-server-side-admin-check.md) | Move admin allowlist checks behind a server route | App | P2 | M | MED | — | DONE locally 2026-07-07 — client guard now uses `/api/admin/check` with a fail-closed timeout; focused app tests pass |
| [010](010-atomic-social-run-finish-counters.md) | Make social job finish and run-counter updates atomic | Backend | P2 | M | HIGH | 004 | DONE locally 2026-07-07 — job finish and counter updates share one transaction; rollback regression passes |
| [011](011-surface-realtime-publish-failures.md) | Stop silently swallowing realtime publish failures | Backend | P3 | S | LOW | — | DONE locally 2026-07-07 — realtime helpers return failure and log exception context without payload text |
| [012](012-isolate-modal-jobs-reload-pollution.md) | Isolate modal job reload pollution in tests | Backend | P3 | S | LOW | 001 | DONE locally 2026-07-07 — reload guard restores patched modules/env; `tests/test_modal_jobs.py` passes |
| [013](013-harden-debug-log-secret-and-size.md) | Harden the debug-log endpoint secret check and body size | App | P3 | S | LOW | — | DONE locally 2026-07-07 — timing-safe shared-secret check and 64KB body cap added; focused tests pass |
| [014](014-run-backend-container-as-non-root.md) | Run the backend container as a non-root user | Backend | P3 | S | LOW | — | DONE locally 2026-07-07 — Dockerfile uses non-root `trr` user; container check blocked because Docker daemon is unavailable |
| [015](015-fail-closed-cors-in-production.md) | Fail closed when production CORS origins are not configured | Backend | P3 | S | LOW | — | DONE locally 2026-07-07 — deployed runtime now requires `CORS_ALLOW_ORIGINS`; startup tests pass; Modal deploy pending |
| [016](016-safe-admin-query-integer-parsing.md) | Add safe integer parsing for admin route query params | App | P3 | S | LOW | — | DONE locally 2026-07-07 — shared bounded integer parser migrated across obvious admin routes; focused app tests pass |
| [017](017-extract-first-social-analytics-module.md) | Extract the first stable slice from social season analytics | Backend | P3 | L | HIGH | — | DONE locally 2026-07-07 — source-scope helpers extracted and re-exported; import/tests/ruff pass |
| [018](018-refresh-supabase-jwt-issuer-decision.md) | Refresh the Supabase JWT issuer and audience decision | Backend | P3 | M | MED | — | DONE locally 2026-07-07 — issuer/audience behavior pinned in tests and docs; focused checks pass |
| [019](019-clean-tracked-root-artifacts.md) | Remove tracked root scratch artifacts | Both | P3 | S | LOW | — | DONE locally 2026-07-07 — tracked scratch reports/root wrappers removed; narrow ignore rules added |
| [020](020-move-vision-deps-out-of-base-requirements.md) | Move vision-only dependencies out of base backend requirements | Backend | P3 | S | LOW | — | DONE locally 2026-07-07 — base lock slimmed, vision lock keeps vision stack; import/tests pass; Modal deploy pending |
| [021](021-close-instagram-runtime-stub-gaps.md) | Close remaining Instagram runtime stub gaps | Backend | P3 | S | LOW | — | DONE locally 2026-07-07 — experimental runtime/unavailable behavior pinned in tests |
| [022](022-add-backend-ci-uv-cache.md) | Add uv caching and remove duplicate backend CI install work | Backend | P3 | S | LOW | — | DONE locally 2026-07-07 — uv cache config added to backend CI; YAML parses |
| [023](023-add-proxy-routing-characterization-tests.md) | Add characterization tests for Next proxy routing | App | P3 | M | LOW | — | DONE locally 2026-07-07 — route matrix characterization tests added without proxy behavior changes |
| [024](024-align-env-example-and-pnpm-overrides.md) | Align backend env examples and app pnpm override documentation | Both | P3 | S | LOW | — | DONE locally 2026-07-07 — JWT/CORS env docs aligned and pnpm override rationale added |
| [025](025-modal-runtime-injector-operator-overrides.md) | Let operators override tunable social knobs (injector `setdefault` allowlist) | Backend | P1 | M | MED | — | DONE (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `e1fbce52` on `advisor/025-modal-runtime-injector-operator-overrides`; plan reconciled to set `TRR_MODAL_RUNTIME_SCHEDULER_ENABLED=1` for the modal_jobs import guard |
| [026](026-avatar-mirror-ssrf-guard.md) | Route scraped avatar/profile-pic downloads through the SSRF guard | Backend | P1 | S | LOW | — | DONE (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `effa1641` on `advisor/026-avatar-mirror-ssrf-guard` |
| [027](027-season-analytics-comment-rollups.md) | Use comment-rollup table for Instagram season week/coverage counts | Backend | P2 | M | MED | — | DONE (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `5da49073` on `advisor/027-season-analytics-comment-rollups`; coverage site rewritten SQL→Python (behavior-preserving, `is_missing` is NOT NULL), parity-tested |
| [028](028-converge-decodo-posts-proxy-gating.md) | Require explicit Decodo opt-in on TikTok/Threads posts proxies | Backend | P1 | S | MED | — | DONE (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `b2878ca2` on `advisor/028-converge-decodo-posts-proxy-gating` |
| [029](029-cookie-refresh-browser-leak.md) | Close the browser on the cookie-refresh error path | Backend | P2 | S | LOW | — | DONE (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `d6ce021b` on `advisor/029-cookie-refresh-browser-leak` (+2 incidental formatting-only lint fixes in-file) |
| [030](030-inline-ingest-aggregate-failures.md) | Stop inline multi-platform ingest swallowing all-but-first failure | Backend | P2 | S | LOW | — | DONE (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `5ebd2925` on `advisor/030-inline-ingest-aggregate-failures` (worktree, awaiting user merge) |
| [031](031-redact-instagram-sessionid-logs.md) | Log an Instagram sessionid fingerprint instead of the raw prefix | Backend | P2 | S | LOW | — | DONE (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `8c2b2ca7` on `advisor/031-redact-instagram-sessionid-logs` |
| [032](032-socials-router-error-detail-leak.md) | Stop leaking raw exception text from socials 500/502 responses | Backend | P2 | M | LOW | — | DONE (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `d6ec83fe` on `advisor/032-socials-router-error-detail-leak`; 53 sites sanitized (scope-clean, route-shape test green) |
| [033](033-comments-recovery-endpoint-tests.md) | Merge-blocking tests for comments resume & auth-repair recovery | Backend | P2 | M | LOW | — | DONE (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `67951e9a` on `advisor/033-comments-recovery-endpoint-tests` (test-only) |
| [034](034-instagram-reel-permalink-normalization.md) | Normalize Instagram Reel permalinks in admin post/comment UIs | Backend + App | P2 | M | MED | — | DONE locally 2026-07-08 — helper + backend read-model fallbacks fixed; focused backend tests/ruff pass; Chrome proves `/reel/DaCAsaUhOYw/` lands on `/reels/DaCAsaUhOYw/`; Modal deploy blocked by unrelated dirty backend tree |
| [035](archive/035-media-url-safety-ssrf-hardening.md) | Close loopback/fail-open holes in the media-URL SSRF guard | Backend | P1 | M | MED | — | DONE & ARCHIVED (executed+approved 2026-07-08, GPT-5.5 HIGH via Codex, after 1 revision — plan cited a base-absent test file) — commit `f4a87c76` on `advisor/035-media-url-safety-ssrf-hardening` |
| [036](archive/036-reddit-worker-loop-resilience.md) | Keep the Reddit refresh drainer alive after a failed run | Backend | P1 | S | LOW | — | DONE & ARCHIVED (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `fd07a1a2` on `advisor/036-reddit-worker-loop-resilience`; loop continues past a failed run + supervisor respawn added |
| [037](archive/037-worker-pool-sigterm-graceful-shutdown.md) | Handle SIGTERM in the social worker pool (stop orphaning grandchild workers) | Backend | P1 | M | MED | — | DONE & ARCHIVED (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `7e5a9a28` on `advisor/037-worker-pool-sigterm-graceful-shutdown`; SIGTERM→graceful + `start_new_session` + process-group kill |
| [038](archive/038-threads-transient-error-auth-cooldown.md) | Stop transient Threads transport errors triggering an escalating auth cooldown | Backend | P2 | S | LOW | — | DONE & ARCHIVED (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `14ff1913` on `advisor/038-threads-transient-error-auth-cooldown` (`auth_failed=False` on transport errors; runner untouched, branch order already correct) |
| [039](archive/039-youtube-reply-loop-termination-guard.md) | Guard the YouTube reply-fetch loop against a non-advancing continuation token | Backend | P2 | S | LOW | — | DONE & ARCHIVED (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `a8bb0640` on `advisor/039-youtube-reply-loop-termination-guard` |
| [040](archive/040-harden-destructive-social-operator-scripts.md) | Harden destructive social operator scripts (retire race + delete-by-default) | Backend | P2 | S | LOW | — | DONE & ARCHIVED (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `95a7d067` on `advisor/040-harden-destructive-social-operator-scripts`; each retire UPDATE guard matches its selection status set exactly |
| [041](archive/041-session-key-permission-reconciliation.md) | Reconcile at-rest 0600 perms for social sessions + service-account keys | Backend | P2 | S | LOW | — | DONE & ARCHIVED (executed+approved 2026-07-08, GPT-5.5 HIGH, after 1 revision — plan's no-keys-resolver fallback was mis-filed under STOP) — commit `acafaf33` on `advisor/041-session-key-permission-reconciliation`; session-root only, `keys/` deferred (no central resolver). **Operator action still required: rotate the exposed IG sessions + 3 GCP keys** |
| [042](archive/042-twitter-interaction-count-suppression.md) | Stop Twitter interaction "duplicate" mis-count from permanently suppressing real repair candidates | Backend | P2 | M | MED | — | DONE & ARCHIVED (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `730480cd` on `advisor/042-twitter-interaction-count-suppression`; `__trr_inserted` now separates inserts/duplicates/skips; test proves 51 skips no longer inflate `duplicate_count` |
| [043](archive/043-socialblade-authenticated-history-login-retry.md) | Stop SocialBlade re-logging-in + false-failing complete 14/30/31-day histories | Backend | P2 | S | MED | — | DONE & ARCHIVED (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `51732723` on `advisor/043-socialblade-authenticated-history-login-retry`; period-regex now gated on non-authenticated source; unauth-short still retries |
| [044](archive/044-youtube-view-count-runs-fallback.md) | Fix the unreachable `runs` fallback in the YouTube view-count parser | Backend | P3 | S | LOW | — | DONE & ARCHIVED (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `56b51201` on `advisor/044-youtube-view-count-runs-fallback`. Follow-up noted: the like-count parser at `youtube/scraper.py:4220` has the identical dead-fallback pattern |
| [045](archive/045-naive-datetime-window-filters.md) | Normalize naive datetimes to UTC in the YouTube + Twitter scrape window filters | Backend | P3 | S | LOW | — | DONE & ARCHIVED (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `c2f713db` on `advisor/045-naive-datetime-window-filters`; YT coerces in `__post_init__`, Twitter reuses `_window_bound_timestamp` |
| 046 | *(reserved)* Facebook reply `parent_source_comment_id` threading + dead `document_fetcher_factory` DI seam | Backend | — | — | — | — | RESERVED for backlog C41/BUG-18 — not promoted: the high-value parent-threading half needs a captured FB SSR fixture to derive the parent-fbid pattern (not reliable weak-executor work); the dead-DI half alone is low leverage |
| [047](archive/047-reddit-http-client-state-lock.md) | Lock the shared Reddit HTTP client's cooldown + OAuth-token state | Backend | P2 | M | MED | — | DONE & ARCHIVED (executed+approved 2026-07-08, GPT-5.5 HIGH) — commit `4a9afb35` on `advisor/047-reddit-http-client-state-lock`; state lock added, no I/O held under lock; token-bucket deferred |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

### 2026-07-08 Codex audit addendum for plans 025-033

Codex re-audited the Claude/Fable 5 plan artifacts and executor outputs on
2026-07-08 using Plan Architect, DebugPro, and Scrapling routing rules.

- Plan file layout lint passed:
  `node .../plan-architect/1.0.0/scripts/plan-grader-lint.js plans/025-... plans/033-...`.
- The advisor branches and commits listed for **025-033** exist and are reachable
  in `TRR-Backend`; each branch worktree is still present under the Claude
  scratchpad.
- These are **branch-complete**, not merged into the current `TRR-Backend` branch
  (`codex/publish/trr-backend/20260705-174033` still points at `8ea7aa1a`).
  Treat the fixes as awaiting user merge/integration until the commits are
  merged or cherry-picked into the active backend branch.
- `executor-report-030.md` is a stale stopped report from the first sandboxed
  commit attempt (`index.lock: Operation not permitted`). The later
  `commit-probe-030.md` records `COMMITTED 5ebd2925`; the branch is the
  authoritative proof for plan 030.
- Modal follow-through is still not complete. Plans **025** and **028** directly
  touch `modal_jobs.py`; plans **026-033** are backend/scraper/API/test changes
  that still need normal merge validation before any Modal/backend deploy.
- TRR-APP build is not applicable to **025-033** because these nine plans are
  backend-only. Do not use app build status as evidence for this batch.

### 2026-07-08 active-backend implementation addendum

Codex integrated the plan **025-033** advisor changes into the active dirty
`TRR-Backend` worktree on `codex/publish/trr-backend/20260705-174033`.

- Implementation was applied as working-tree edits instead of cherry-picking
  because the backend tree already had many unrelated uncommitted changes.
- The implementation includes the planned source/test changes for **025-033**
  plus one fixture update for the already-present
  `/catalog/runs/{run_id}/media/requeue` route, which was required to make the
  current route-shape guard match the dirty router.
- Focused validation passed:
  `TRR_MODAL_RUNTIME_SCHEDULER_ENABLED=1 .venv/bin/python -m pytest ...` ->
  `122 passed`.
- Touched-file lint passed:
  `ruff check ...` -> `All checks passed!`.
- Modal/backend deploy follow-through is still pending; this is local
  integration only.

> **Plans 025–033 are new (2026-07-08 social-focus deep audit).** They are
> independent of each other and of 001–024, and can be executed in any order,
> with two coordination notes: **028 and 025 both edit `modal_jobs.py`** (025
> changes the injector function + adds a tunable-key set; 028 adds one dict
> entry — no textual conflict); and **027 is the one higher-risk change** (it
> edits the 68.7K-line god module) — its mandatory rollup-vs-raw parity test is
> the guardrail. Recommended first batch by leverage: **025, 026, 028** (the P1
> keystone + two P1 safety fixes), then 027, then the clean correctness/security
> fixes 029–032, then the 033 test net. See the "2026-07-08 social-focus deep
> audit" section near the end of this file for the full findings table, the
> ranked backlog of confirmed-but-unplanned findings, and direction spikes.

## Dependency notes

- **004 depended on 001**: the dependency is now satisfied locally by the
  `test_modal_jobs.py` env-restoration fixture, and 004's focused tests plus
  `tests/api` pass.
- 002, 003, 005, 006, 007, 008, 009, 011, 013, 014, 015, 016, 018, 019, 020, 021, 022, 023, 024 are independent and can be executed in parallel (they touch
  disjoint files across the two repos).
- 010 depends on 004 because the run-lifecycle characterization tests must be in
  place before changing the hot finish/counter write path.
- 012 depends on 001 because it assumes the pristine-env fixture from the
  earlier modal_jobs isolation work exists.
- Historical recommended first batch before plans 007-024 were implemented:
  **001, 002, 003**. Then 006, 004, 005.
- The full ranked backlog B1-B18 is now promoted into plans **007-024**.

## Implementation phases used for plans 007-024

These phases were used for the promoted backlog implementation pass. Within a
phase, plans were run in the listed order unless a live drift check said
otherwise.

### Phase 1 — Security and config boundaries

Tighten auth, debug, deployment, CORS, and documented environment behavior before
larger app/backend changes.

1. [018](018-refresh-supabase-jwt-issuer-decision.md) — refresh the Supabase JWT issuer/audience decision.
2. [009](009-server-side-admin-check.md) — move admin allowlist checks behind a server route.
3. [013](013-harden-debug-log-secret-and-size.md) — harden debug-log shared-secret and body-size handling.
4. [015](015-fail-closed-cors-in-production.md) — fail closed for missing production CORS origins.
5. [014](014-run-backend-container-as-non-root.md) — run the backend container as non-root.
6. [024](024-align-env-example-and-pnpm-overrides.md) — align env examples and pnpm override documentation.

### Phase 2 — Backend reliability and tooling hygiene

Reduce test/runtime noise and speed up backend feedback before touching hotter
runtime paths.

1. [012](012-isolate-modal-jobs-reload-pollution.md) — isolate remaining modal_jobs reload pollution.
2. [011](011-surface-realtime-publish-failures.md) — surface realtime publish failures.
3. [021](021-close-instagram-runtime-stub-gaps.md) — close remaining Instagram runtime stub gaps.
4. [020](020-move-vision-deps-out-of-base-requirements.md) — move vision-only deps out of base requirements.
5. [022](022-add-backend-ci-uv-cache.md) — add backend CI uv caching.
6. [019](019-clean-tracked-root-artifacts.md) — remove tracked root scratch artifacts.

### Phase 3 — Admin app route correctness and coverage

Make the admin routing surface easier to trust before optimizing page behavior.

1. [016](016-safe-admin-query-integer-parsing.md) — add safe integer parsing for admin query params.
2. [023](023-add-proxy-routing-characterization-tests.md) — add Next proxy routing characterization tests.

### Phase 4 — Admin social and cast-screentime performance

Improve operator-facing latency in the admin social and cast-screentime flows.

1. [008](008-parallelize-social-landing-payload.md) — parallelize independent social landing payload phases.
2. [007](007-async-cast-screentime-video-import.md) — queue cast-screentime remote video imports.

### Phase 5 — Social run lifecycle and architecture

Handle the highest-risk social data path after the guardrails and tests above
are in place, then take the first extraction slice.

1. [010](010-atomic-social-run-finish-counters.md) — make social job finish and counter updates atomic.
2. [017](017-extract-first-social-analytics-module.md) — extract the first stable social analytics module slice.

## 2026-07-07 reconcile notes

- Commit-level drift checks for plans **001-006** still resolve cleanly.
- Uncommitted backend changes overlap the general screentime/social analytics
  area, but not the source excerpts that drive plans 001, 002, 004, or 005.
- `make app-validate-quick` passed locally on 2026-07-07.
- Backend import gate `.venv/bin/python -c "import api.main"` passed locally on
  2026-07-07.
- Backend `tests/api` passed locally on 2026-07-07:
  `1104 passed, 12 warnings in 172.85s`.

## 2026-07-07 FINAL consolidation (working tree is authoritative)

Per user decision, all six plans were consolidated into the **working tree** and
the advisor branches were retired. Current state:

- **All 6 changes live in the TRR-Backend / TRR-APP working trees** (uncommitted,
  alongside other in-progress work) — no more `advisor/*` branches or worktrees
  (all removed 2026-07-07).
- Backend: 002 guard applied to `cast_screentime.py` + `screenalytics_runs.py`
  (+ rejection tests); 001 (`tests/conftest.py` + `test_modal_jobs.py`); 004
  (`test_run_lifecycle_counters.py`). Focused verification: **66 passed**, ruff clean.
- App: 003 (`getty-local-scrape.ts` + test), 005 (`social-landing-repository.ts`
  + test), 006 (`web-tests.yml`), plus a NEW **no-DB typography fallback**
  (`api/design-system/typography/route.ts` gates on
  `resolvePostgresConnectionCandidates(process.env).length === 0` →
  `buildTypographyStateSnapshot()`), fixing the placeholder-e2e DB-timeout noise.
  Verification: **typecheck ✓, 47 consolidated tests ✓, lint 0 errors**.
- **Production build: BLOCKED by local memory pressure** (`safe-next-build`
  refused: 0.5 GiB free vs 4.0 GiB needed, 13.4 GiB swap) — NOT a code failure.
  Re-run when the machine has memory, or rely on CI (fresh runner). All other
  gates are green.
- Everything is uncommitted — the user stages/commits as they see fit.

## 2026-07-07 plans 007-024 implementation pass

All promoted backlog plans **007-024** were implemented locally with subagent
workstreams and lead integration.

Validation:

- Backend focused suite passed: `169 passed, 12 warnings`.
- Backend lint passed for touched files: `ruff check ...`.
- Backend import gate passed: `.venv/bin/python -c "import api.main"`.
- App focused suite passed: `14 passed`, `102 tests passed`.
- App typecheck passed: `pnpm -C apps/web run typecheck`.
- App lint passed with `0 errors, 6 existing warnings`.
- `git diff --check` passed at workspace root, TRR-Backend, and TRR-APP.

Operational follow-through:

- Docker runtime verification for plan 014 is still blocked because the local
  Docker daemon socket is unavailable.
- Modal follow-through is still blocked. Backend deploy is required for the API,
  runtime, dependency, and Modal-surface changes, but should wait until the TRR
  Modal maintenance owner/deploy surface is cleanly approved.
- Plan 007's queued import is the intended first slice and still uses local
  FastAPI background work; moving it to a durable Modal worker remains the
  crash-safety follow-through before high-volume use.
- TRR-APP production build was not run because current-chat approval was not
  requested.
- Reviewer follow-up fixed the `/api/admin/check` hang risk by adding a
  fail-closed client timeout and abort-path test.

### Follow-ups (2026-07-07, after consolidation)

- **DONE — no-DB typography fallback** (covered "no-DB design-system routes": typography is the ONLY public design-system route that queries the DB).
- **DONE by scope, VERIFIED** — homepage-smoke rename: `homepage-visual-smoke.spec.ts` → `admin-entry-smoke.spec.ts` (it tests the admin entry screen); no dangling references (grep-verified).
- **DONE by scope** — SocialBlade cookie-health normalization: new `apps/web/src/lib/admin/socialblade-cookie-health.ts` canonical normalizer; both `cookies/health` routes return the canonical shape; component + regression test updated. App typecheck now passes after node_modules repair; the specific component regression was not rerun in the 007-024 pass.
- **DEFERRED to backlog**: "move local fixes onto advisor branches" (moot — consolidated to tree instead).

### Environment blocker update (2026-07-07)

- App `node_modules` was repaired by moving stale app-level links that pointed at
  the deleted advisor worktree out of the repo and reinstalling from the
  workspace lockfile. App typecheck and lint now pass.
- Production build is still not run because current-chat approval is required.
- Backend focused validation remains green: focused tests passed, ruff clean,
  and `import api.main` ok.

## 2026-07-07 execute-all reconciliation (git is authoritative — superseded by FINAL consolidation above)

Two `execute all` passes ran on 2026-07-07 (a concurrent run co-edited this
index). A later local continuation resolved the remaining 001/004/006 blocks in
the dirty worktrees. Current ground-truth:

| Plan | Branch | Head commit | Verdict |
|---|---|---|---|
| 001 | local dirty worktree | (uncommitted) | DONE locally — target file now passes in isolation |
| 002 | advisor/002-sql-column-identifier-hardening | `902a69ad` | DONE |
| 003 | advisor/003-getty-prefetch-token-path-traversal | `343c2a96` | DONE |
| 004 | local dirty worktree | (uncommitted) | DONE locally — characterization tests pass |
| 005 | advisor/005-social-progress-lane-counts | `8871b3e5` | DONE |
| 006 | local dirty worktree | (uncommitted) | DONE locally — e2e baseline and non-blocking CI step verified |

**All 6 plans now have a local resolution path; 001/004/006 are uncommitted local
worktree changes, while 002/003/005 remain advisor-branch commits awaiting user
merge.**
Note: 002 has two functionally-equivalent commits from the two runs; the branch
points to `902a69ad`. All executor commits were reviewed (scope, diff, tests,
re-run done-criteria). The earlier 001 owner-guard failure is now fixed locally by
restoring Modal runtime env defaults after each test.

Latest local validation on 2026-07-07:
- Backend focused: `ruff check tests/conftest.py tests/test_modal_jobs.py tests/repositories/test_run_lifecycle_counters.py` and `pytest tests/test_modal_jobs.py tests/repositories/test_run_lifecycle_counters.py -q` passed (`53 passed`).
- Backend API: `.venv/bin/python -m pytest tests/api -q` passed (`1104 passed, 12 warnings`).
- App focused: workflow YAML parse, ESLint, TypeScript, and `vitest tests/cast-socialblade-comparison.test.tsx` passed.
- App e2e: CI-style placeholder env `playwright test -c playwright.config.ts` passed (`18 passed, 3 skipped`).

## 2026-07-07 execute-all subagent notes (historical first pass — superseded above where noted)

- 001: first pass stopped with no file changes because the target file was red
  in isolation from the Modal maintenance-owner guard. Superseded by the local
  env-restoration fix above.
- 002: approved. Reviewer reran import, repository tests, API tests with
  `TRR_LOCAL_DEV=1`, and ruff. The worktree has an untracked reviewer-created
  `.venv` symlink only; the committed diff is scope-clean.
- 003: approved. Reviewer reran the Getty token unit test, typecheck, and lint;
  lint exits 0 with the existing five warnings outside the edited files.
- 004: first pass skipped because 001 was blocked. Superseded by the local
  characterization tests above.
- 005: approved. Reviewer reran the social landing repository test, typecheck,
  and lint; lint exits 0 with the existing five warnings outside the edited
  files.
- 006: first pass blocked after the executor stopped with no commit. Superseded
  by the local Playwright baseline fixes and non-blocking workflow step above.

## How these were prioritized

Leverage = impact ÷ effort, discounted by confidence and fix-risk. Tiebreakers
applied: prerequisite-unblockers float up (001 unblocks the whole non-`tests/api`
lane); HIGH-confidence security floats above equal-leverage non-security (002,
003); findings with a clean command-based verification story preferred (all six
have one). Every finding in a plan was re-read in the source by the advisor
before the plan was written — subagent line numbers were treated as leads, not
facts.

---

## Vetted findings NOT yet planned (ranked backlog)

These are real, confirmed findings the audit surfaced that were not turned into
plans this round (either larger/riskier than the top 6, or lower leverage). Pick
any to promote to a plan.

| Rank | Finding | Repo | Category | Effort | Risk | Why deferred |
|---|---|---|---|---|---|---|
| B1 | Video-import endpoint blocks a FastAPI worker for minutes (sync `yt-dlp` subprocess + sequential channel resolution w/ 250ms delays + sync `ffprobe` + `requests` S3 download) — `api/routers/admin_cast_screentime.py:566,760,832`. Move to a Modal/background job returning 202+job id. | Backend | perf | L | MED | PROMOTED to [007](007-async-cast-screentime-video-import.md) as a narrower first slice: local queued import using the existing upload-session row, no new queue table. |
| B2 | Social landing payload assembled via 4 sequential await phases; `buildPeopleProfiles`/`buildCastSocialBladeShows` are independent but serial — `social-landing-repository.ts:2657-2764`. Parallelize into the existing `Promise.all`. | App | perf | M | LOW | PROMOTED to [008](008-parallelize-social-landing-payload.md). |
| B3 | `NEXT_PUBLIC_ADMIN_EMAILS` / `NEXT_PUBLIC_ADMIN_UIDS` + a hardcoded admin UID ship in the **public** client bundle — `lib/admin/client-access.ts:13,18`, `lib/admin/constants.ts:12`. Migrate to a server-side `/api/admin/check`. | App | security | M | MED | PROMOTED to [009](009-server-side-admin-check.md). |
| B4 | `_finish_job` commits the job-status change and the run-counter update in **separate transactions** — `social_season_analytics_impl.py:13619` then `run_lifecycle.py:1130`. Atomicity refactor of the hottest write path. | Backend | correctness | M | HIGH | PROMOTED to [010](010-atomic-social-run-finish-counters.md). |
| B5 | `publish_event_sync` runs `asyncio.run(...)` inside a sync FastAPI BackgroundTask and swallows all errors — `discussions.py:52`, `dms.py:46`. WebSocket events for discussions/DMs can silently never deliver. | Backend | correctness | S | LOW | PROMOTED to [011](011-surface-realtime-publish-failures.md). |
| B6 | Second pollution vector beyond 001: `importlib.reload(modal_jobs)` following `monkeypatch.setitem(sys.modules, ...)` (statements begin near `test_modal_jobs.py:596,635,670,1085,1090`; the `setitem` itself is auto-rolled-back, so the residual leak is from the reload). | Backend | tests | S | LOW | PROMOTED to [012](012-isolate-modal-jobs-reload-pollution.md). |
| B7 | Debug-log endpoint uses non-constant-time secret comparison (`===`) — `app/api/debug-log/route.ts:52`; the internal-admin module already uses `timingSafeEqual`. Also add a body-size cap. | App | security | S | LOW | PROMOTED to [013](013-harden-debug-log-secret-and-size.md). |
| B8 | Dockerfile runs as root (no `USER`) — `TRR-Backend/Dockerfile`. Add a non-root user. | Backend | security | S | LOW | PROMOTED to [014](014-run-backend-container-as-non-root.md). |
| B9 | CORS falls back to `allow_origins=["*"]` when `CORS_ALLOW_ORIGINS` is unset — `api/main.py:513`. Fail-closed / require explicit origins in prod. | Backend | security | S | LOW | PROMOTED to [015](015-fail-closed-cors-in-production.md). |
| B10 | `parseInt` on route `limit`/`offset` yields `NaN` that survives the `Math.min(Math.max(1, raw), MAX)` clamp (`Math.max(1, NaN) === NaN`) → 500 instead of 400 — `api/admin/trr-api/people/route.ts:26` and ~5 similar routes. Add a shared `safeParseInt`. | App | correctness | S | LOW | PROMOTED to [016](016-safe-admin-query-integer-parsing.md). |
| B11 | 68,027-line god module `social_season_analytics_impl.py` (380× the ~178-line median, ~200 importers) + god-routers `admin_person_images.py` (17.2K) and `socials/__init__.py` (9.4K). Incremental extraction. | Backend | tech-debt | L | HIGH | PROMOTED to [017](017-extract-first-social-analytics-module.md). |
| B12 | JWT `verify_aud: False` **and** `verify_iss: False` with manual issuer check + a `service_role`/`"supabase"` carve-out — `security/jwt.py:143`. | Backend | security | M | MED | PROMOTED to [018](018-refresh-supabase-jwt-issuer-decision.md). |
| B13 | Dead/stray tracked artifacts at workspace root: `plan.md`, `instagram-backfill-debug-report.md`, `claude-desktop-ui-mod-plan.html`, `design-docs-agent-comparison.html` (~1,463 lines); backend `test_connection.py`, `backfill_tmdb_show_details.py`, `resolve_tmdb_ids_via_find.py` at repo root. `git rm --cached` + `.gitignore`. | Both | tech-debt | S | LOW | PROMOTED to [019](019-clean-tracked-root-artifacts.md). |
| B14 | `deepface`/`numpy`/`opencv-python`/`sympy` pinned in base `requirements.in` but only used by the vision lane (sympy imported nowhere) — bloats every dev/CI install. Move to `requirements.modal.vision.in` only. | Backend | deps | S | LOW | PROMOTED to [020](020-move-vision-deps-out-of-base-requirements.md). |
| B15 | Instagram runtime stubs (`browser_use_runtime.py`, `crawl4ai_runtime.py`, `crawlee_runtime.py`) are scaffold-only with stale "TODO before production" checklists yet satisfy the runtime protocol (silent-misuse risk). Add `NotImplementedError` guards or move to `_experimental/`. | Backend | tech-debt | S | LOW | PROMOTED to [021](021-close-instagram-runtime-stub-gaps.md). |
| B16 | Backend CI installs deps from scratch on every job (no uv cache) and runs `test`/`test-py312-canary`/`test-full` with duplicated checkout+install; `test` re-runs the same `tests/api` as the canary — `ci.yml`. Add `cache-dependency-path` to `setup-uv`; consolidate. | Backend | dx/perf | S | LOW | PROMOTED to [022](022-add-backend-ci-uv-cache.md). |
| B17 | `src/proxy.ts` (1,217-line Next.js middleware on every request) has coverage only for host-allowlist routing; screenalytics/brand/social/fandom rewrites untested. Add `proxy-routing.test.ts`. | App | tests | M | LOW | PROMOTED to [023](023-add-proxy-routing-characterization-tests.md). |
| B18 | `.env.example` omits README-documented `SUPABASE_PROJECT_REF` / `SUPABASE_JWT_ISSUER`; `pnpm-workspace.yaml` has undocumented/possibly-stale overrides (`node-domexception` dead on Node 24, `glob`, `json-ptr`, `jwks-rsa>jose`, 21-entry `minimumReleaseAgeExclude`). | Both | dx/deps | S | LOW | PROMOTED to [024](024-align-env-example-and-pnpm-overrides.md). |

Other confirmed-but-minor items (not individually ranked): `SELECT *` on JSONB-heavy
tables in `cast_screentime.py`/`admin_reddit_reads.py` (projection wins);
duplicate HTTP clients `requests` (66 sites) + `httpx` (12) — standardize new code
on `httpx`; internal error strings forwarded to clients via
`HTTPException(detail=f"...{e}")` (surveys.py:315 is public-facing); WS JWT passed
as query param (`ws.py:123,201`); `.nvmrc` (`24`) vs pnpm `nodeVersion` (`24.14.0`)
mismatch; `SocialAccountProfilePage.tsx` has 49 `useEffect`s (stale-closure risk;
one such timer bug was just fixed in `83778e5c`); per-keystroke refetch in
`InstagramCommentsPanel.tsx` (no debounce).

---

## Direction findings (options for the maintainer — not ranked against bugs)

Every suggestion below cites repo evidence. Strategy is yours; these are grounded
options with honest trade-offs. Selected ones become **design/spike** plans, not
build-everything plans.

- **D1 — Public social surface from the existing pipeline.** The admin app has a
  deep social-analytics tree (`admin/social/[platform]/[handle]/` with posts,
  comments, catalog, collaborators, hashtags, SocialBlade, Reddit drill-downs),
  but `/social-media` explicitly "no longer renders the admin dashboard" and the
  public `[showId]/social/` routes are stubs. The ingestion pipeline produces a
  rich dataset only one admin can see. *Impact*: high — a public cast/show social
  leaderboard is low-marginal-cost for a reality-TV platform. *Trade-off*: needs
  public-safe read endpoints (the data exists under `admin_social_posts.py` /
  `admin_socialblade.py` behind admin auth) and a review of what's safe to expose
  publicly (Instagram ToS/privacy). Effort: M–L (coarse).

- **D2 — Screentime × social correlation.** `CastScreentimePageClient.tsx`
  already has a `social_youtube_row` import mode, and both screentime runs and
  social metrics are keyed by person. No combined view exists (e.g. "cast with
  most screen time vs. highest follower growth during the episode window").
  *Impact*: this cross-correlation is the differentiated insight the platform is
  uniquely positioned for. *Trade-off*: read-only analytics on existing data;
  main unknown is whether the person_id FKs align cleanly (verify in a spike).
  Effort: M (coarse).

- **D3 — Surveys / DMs / discussions have complete backends, no public UX.**
  `surveys.py` (420 lines, live aggregate results), `dms.py` (336, conversations
  + WS), `discussions.py` (463, episode threads + reactions) are fully
  implemented; the frontend only re-exports the admin surveys page. *Impact*:
  three engagement features whose backend investment is already made. *Trade-off*:
  needs public user accounts (auth is admin-only today), moderation, and abuse
  prevention. Start with **surveys** (read-heavy, lowest-risk). Effort: L per
  feature (coarse).

- **D4 — Parked/placeholder surfaces: decide build-or-delete.** Flashback game is
  fully built but `isLiveEnabled: false` with redirect-only routes
  (`lib/admin/games.ts:55`) while 7 gameplay components sit unrendered;
  `realations/page.tsx` is an empty purple-grid placeholder. *Impact*: either
  dead weight or intent signals. *Trade-off*: S to remove, M–L to ship. Deciding
  is cheap; leaving them ambiguous costs navigation/build noise. Effort: S–L
  (coarse).

---

## Findings considered and rejected (so they are not re-audited)

- **Dockerfile `COPY . .` bakes `.env`/`keys/` into the image** (backend security
  agent SECURITY-08): **REJECTED — false positive.** The real
  `TRR-Backend/.dockerignore` already excludes `keys/`, `.env*`, `logs/`,
  `debug_html/`, `out/`, `.supabase/`. `keys/` is also git-ignored and untracked.
  The agent read a different/stale ignore file. (Adding `data/` for image size is
  a trivial nicety, not a security finding.)
- **"No `.github/workflows/` exists / no CI"** (debt agent DEPS-05 premise):
  **REJECTED — incorrect.** `TRR-Backend/.github/workflows/ci.yml` exists with 5
  jobs incl. a `test-py312-canary`. The underlying Python-3.11-pin observation is
  noted in B-backlog context, but the "no CI" claim is false.
- **`InternalAdminUser = None` default is an auth bypass** (SECURITY-10):
  **REJECTED as a vulnerability.** FastAPI always executes `Annotated[..., Depends()]`
  regardless of the `= None` default, and `require_internal_admin` raises on
  failure — so the default is dead code, not a bypass. It is a readability wart
  (mechanical `= None` removal across ~266 sites) at most, not a security finding.
- **Core-table RLS is open-read** (SECURITY-09): **REJECTED — by design.** Public
  browse surface is intentionally read-only via `USING (true)`; backend admin
  writes use direct SQL that bypasses RLS. Worth an ADR note, not a fix.
- **Adaptive scrape control-plane architecture**: settled by
  `TRR-Backend/docs/adr/0001` — only implementation bugs in it are findings
  (e.g. the counter-drift cluster B4/004), not the design.
- **Rollup trigger / posts-pacing advisory lock**: already applied to the DB per
  project history — not re-reported.

---

## Notes for `reconcile` / future runs

- This is the first advisor run; `recon.md` is the cache. Spot-verify its commands
  before trusting them next time.
- The two remaining pollution vectors (001 fixture + B6 `sys.modules` injection)
  should both land before anyone promotes the `test-full` backend CI job from
  `continue-on-error: true` to blocking — that promotion is the real prize behind
  001/B6 and deserves its own plan once the full non-browser/non-vision/non-live
  lane is green in CI.

---

# 2026-07-08 social-focus deep audit

Second `/improve-more` run (`deep`), scoped to the **social-media pipeline**
(Modal jobs, Decodo/proxy, Scrapling transports, Supabase social contracts, the
admin social surface). Eight read-only category subagents fanned out over
`trr_backend/socials/**`, `modal_jobs.py`, `modal_dispatch.py`,
`api/routers/socials/**`, the social Supabase migrations, and the app-side social
routes. Heads unchanged since the first run (root `fb76b5b`, Backend `8ea7aa1a`,
App `83778e5c`); working trees still dirty and **authoritative**. Every finding
below was re-read in the live source by the advisor before it was tabled or
planned. Plans **025–033** were written from this run.

## ⚠️ Repo visibility (security plans)

All three repos are **public**. Plans 026 (avatar SSRF), 028 (Decodo gating),
031 (sessionid logging), and 032 (exception-detail leak) describe
vulnerability *locations and remediations* but contain **no secret values and no
runnable exploit strings** — same remediation-focused posture as the already
-published 002/003. Bounded exposure, but before `git add`/push decide as with
001–024: gitignore `/plans/`, accept the public descriptions, or move the four
security plans somewhere VCS-ignored. This run was non-interactive, so no
`--issues` were created.

## Vetted findings (leverage-ranked; ✅ = turned into a plan this run)

| # | Finding | Cat | Impact | Effort | Risk | Conf | Evidence | Plan |
|---|---------|-----|--------|--------|------|------|----------|------|
| DX-01 | Modal injector `os.environ[key]=value` (not setdefault) clobbers operator-set proxy/pagination knobs at every container boot | dx/correctness | "turn comments proxy off" impossible without a code deploy; secret-set knobs are dead-on-arrival | M | MED | HIGH | `modal_jobs.py:689-691,750`; dict `:289-428`; reads `public_mode.py:19`,`public_probe.py:43` | ✅ 025 |
| SEC-01 | Scraped avatar/profile-pic URLs fetched server-side with only a `startswith` check, redirects followed, no host/IP guard | security | SSRF to metadata/loopback/internal from a hostile profile | S | LOW | HIGH | `social_season_analytics_impl.py:18581,18869,19068,19103`; guard exists `media_url_safety.py` | ✅ 026 |
| DEBT-02 | Decodo activation gate drifted: TikTok activates on empty provider, Threads auto-enables on creds — Instagram requires explicit opt-in (with a safety comment) | correctness/security | a stale residential proxy can mask healthy auth as blocked on TikTok/Threads | S | MED | HIGH | `tiktok/posts_scrapling/proxy.py:64` vs `instagram/…/proxy.py:244` vs `threads/…/proxy.py:44-49` | ✅ 028 |
| PERF-01 | Season week/coverage builders `count(*)` raw comment tables (~7 sites) instead of the trigger-maintained rollups the profile path already uses | perf | full comment-slice scan per poll (~100K rows) on the busiest admin surface; ~90s/20s cadence | M | MED | HIGH | `social_season_analytics_impl.py:55797,56121,57698,53886`; helper `read_models/account_profile/common.py:193` | ✅ 027 (Instagram) |
| BUG-02 | `open_cookie_refresh_context` leaks the browser if `new_context` raises (cleanup owned by the not-yet-returned wrapper) | bug | leaked Chromium processes per failed auth refresh until container recycle | S | LOW | HIGH | `browser_cookie_refresh.py:415-417,51` | ✅ 029 |
| BUG-06 | Inline multi-platform ingest `for f in futures: f.result()` drops all-but-first platform's exception | bug | second+ platform failures silently lost; contradicts CONTEXT.md partial-success contract | S | LOW | HIGH | `inline_ingest.py:55,70` | ✅ 030 |
| SEC-03 | Instagram `sessionid[:8]` logged at info to an off-box log store | security | partial session-token material in central logs | S | LOW | HIGH | `instagram/scraper.py:879,972` | ✅ 031 |
| SEC-02 | ~17 `HTTPException(status_code=500/502, detail=str(exc))` sites leak raw internal error text (incl. proxy upstream on the 502) | security | DB/hostname/proxy detail disclosed in API responses | M | LOW | HIGH | `api/routers/socials/__init__.py:3750,4750,…` (80 `str(exc)` total; 500/502 subset in scope) | ✅ 032 |
| TEST-03 | Comments resume & auth-repair recovery endpoints have zero tests anywhere; repair fires a cookie-refresh background task | tests | recovery path + cookie-refresh regressions ship green | M | LOW | HIGH | `api/routers/socials/__init__.py:5326,5374`; pipeline `comments/instagram.py:6482,7303,7363` | ✅ 033 |
| BUG-01 | `_touch_job_dispatch_metadata` read-modify-writes the `dispatch` sub-object with no `FOR UPDATE`; 13 dispatcher/recovery callers unlocked | correctness | concurrent dispatch/sweep lose counter/invocation-id updates on the hottest write path | S–M | LOW | MED | `social_season_analytics_impl.py:12930,12951,12977`; `control_plane/dispatch_runtime.py:771,872,894,938` | backlog C1 |
| DEBT-04 | Two error taxonomies disagree: `login_required` retryable in `_classify_job_error`, terminal in crawlee `classify_exception` | correctness | same failure retries in one lane, dead-letters in another | M | MED | HIGH | `social_season_analytics_impl.py:31294,31307` vs `crawlee_runtime/error_taxonomy.py:120` | backlog C2 |
| BUG-04 | Deterministic `must appear in the GROUP BY clause` error classified retryable in deferred-comments followup | correctness | burns retry budget on a query-structure defect; masks a real malformed aggregate | M | MED | `run_lifecycle.py:304-307,397,635` | backlog C3 |
| DX-03 | Scraper/worker logs carry no run_id/job_id correlation; `bind_trace_id` only called in `api/main.py` | dx | can't filter the log firehose to one stalled run without a code change | S–M | LOW | HIGH | `observability.py:330`; `job_runner.py` 2/43 logs, `fetcher.py` 0/23 | backlog C4 |
| DX-04 | Decodo proxy spend invisible (WARNING-only, budget defaults `0.0` so it never fires); no endpoint/table | dx/observability | proxy exhaustion (a named incident) has no pre-stall signal | M | LOW | HIGH | `decodo_usage.py:229-231`; cron `modal_jobs.py:1534` | backlog C5 |
| DEPS-03 | Rollup migration template ships `search_path = social, public`, forcing a per-platform advisor hardening follow-up | deps/security | each new platform re-introduces the mutable-search_path advisor finding | S | LOW | HIGH | `20260610190000:29,63` hardened later by `20260702184229` | backlog C6 |
| DEPS-01 | Render/Docker prod image installs the base lock → bundles playwright+pytest+scrapling+crawlee the API never runs (Modal already lean) | deps/security | fat prod image, slower cold start, wider surface | M | MED (render standby) | MED | `Dockerfile:19-20`; lean proof `modal_jobs.py:502,757` | backlog C7 |
| DEPS-02 | Three byte-identical rollup+trigger migrations (instagram/tiktok/youtube) | deps/debt | every new platform = 157 lines of copy-paste DDL; fixes need triplication | M | MED | HIGH | diff of `20260610190000` / `20260625172000` / `20260702181000` = identical | backlog C8 |
| DEBT-01 | 12+ hand-rolled retry/backoff implementations; `_retry.py` (the intended unifier) has 1 importer; catalog_ingest uses linear not exponential | debt | a rate-limit/jitter fix must touch a dozen sites; linear copy starves recovery | L | MED | HIGH | `_retry.py:1-17`; `catalog_ingest.py:1501,1745`; `scrapling_transport.py:25-26` | backlog C9 |
| DEBT-05 | Next god-module slice: cohesive cookie loader/validator/registry cluster (`social_season_analytics_impl.py` ~8823–10010) with mapped importers | debt (enabler) | posts_scrapling lanes pay a 68.7K-line import + lazy-import workarounds to fetch cookies | M | MED | HIGH | loaders 8823–9895, validators 9091–9802, registry 9899–10010; importers `control_plane/__init__.py:112`, `tiktok/…/session.py:42` | backlog C10 |
| DEBT-03 | `socialblade/proxy.py` imports Instagram-private `_proxy_sessions`; forked `apply_decodo_session_affinity` / `cookies_to_scrapling` copies | debt | cross-platform coupling; sticky-session/cookie fixes drift | S | LOW | HIGH | `scrapling_transport.py:308,358` vs `instagram/_proxy_sessions.py:12`, `tiktok/…/session.py:21` | backlog C11 |
| SEC-04 | Reddit-admin audit actor taken from spoofable `X-TRR-Admin-User-*` headers before the authenticated principal | security | destructive Reddit admin ops attributable to a spoofed identity | S | LOW | HIGH | `admin_reddit_reads.py:194-212,345,553` | backlog C12 |
| PERF-02 | Cached week-detail poll deep-copies the full payload 3× (incl. a redundant TTL re-set on hits) | perf | pure CPU per poll on populated weeks; no correctness value | S | LOW–MED | HIGH | `api/routers/socials/__init__.py:8771,8812,8814`; `_analytics_cache.py:192,197` | backlog C13 |
| BUG-05 | Failed deferred-comments followup leaves parent run `completed`; self-heal sweep default-off | correctness | comments backfill silently skipped on transient launch failure (alerted, not auto-recovered) | S | MED | MED | `run_lifecycle.py:420,445,624,1644` | backlog C14 |
| DEPS-05 | `curl_cffi` (compiled libcurl-impersonate) pinned in lean+vision Modal lanes but only the browser-lane TikTok scraper imports it | deps | image size + cold-start on API/vision images | S | LOW | MED | `requirements.modal.lean.in:8`; sole import `tiktok/http_client.py:191` | backlog C15 |
| DOC-01 | `env-contract.md` documents 4 of 188 live social env vars (credentials, GraphQL doc-id overrides absent) | docs | operator can't learn the break-glass knobs (doc-id rotation, cookie/proxy) | M | LOW | HIGH | grep of `SOCIAL_/DECODO_/INSTAGRAM_` reads vs `docs/workspace/env-contract.md` | backlog C16 |
| DOC-02 | `.env.example` ships 16 social values the injector silently overrides + omits 46 pinned knobs, unannotated | docs | onboarding template actively wrong (worse than missing) | S | LOW | HIGH | `.env.example:514,553,593,616` vs `modal_jobs.py:289-428` | backlog C17 (pairs with 025) |
| Other | Lower-severity confirmed items — see "Other confirmed-but-minor" below | — | — | — | — | — | — | backlog |

## Ranked backlog (confirmed, not planned this run)

Pick any to promote to a plan (they continue the numbering at 034+). Ordered by
rough leverage.

| Rank | Finding | Repo | Cat | Effort | Risk | Why deferred |
|---|---|---|---|---|---|---|
| C1 | BUG-01 dispatch-metadata RMW race — make the `dispatch` sub-object merge atomic (single `jsonb_set(... || %s::jsonb)` UPDATE or `FOR UPDATE` in the write txn) | Backend | correctness | S–M | LOW | Hottest write path + 13 callers; the safe SQL-level merge needs each caller's counter semantics mapped before a weak executor touches it. High-value; promote with care. |
| C2 | DEBT-04 unify the two error taxonomies (`login_required` retryable-vs-terminal) onto one shared `socials/error_taxonomy.py` | Backend | correctness | M | MED | Changes which failures retry vs dead-letter across lanes; needs recovery-test coverage first. |
| C3 | BUG-04 reclassify `sql_grouping_contract` as terminal AND locate the malformed `p.*`/`fb_comment_count` GROUP BY query | Backend | correctness | M | MED | Reclassify is safe; finding the root query is an investigation. Pairs with C2. |
| C4 | DX-03 bind `run_id`/`job_id` into the trace contextvar at each Modal worker entry (reuse `observability.bind_trace_id`) | Backend | dx | S–M | LOW | Pure observability add; high operational value for debugging one run. |
| C5 | DX-04 surface Decodo spend on the social status API + emit an INFO datapoint per poll; set non-zero default budgets | Backend | dx | M | LOW | Read-only observability; needs a home on the status read model. |
| C6 | DEPS-03 fix the rollup DDL template `search_path` to `social, pg_temp` | Backend | deps/security | S | LOW | End-state already hardened for the 3 existing platforms; this prevents the churn on the next platform (pairs with C8). |
| C7 | DEPS-01 give the Docker/Render image the lean lock (drop playwright/pytest/scrapling/crawlee from prod) | Backend | deps | M | MED | `render.yaml autoDeploy:false` lowers urgency; must confirm no API-startup import of the scraper stack (Modal-lean is strong evidence it's safe). |
| C8 | DEPS-02 collapse the 3 identical rollup migrations into one `social.install_comment_rollup(...)` installer | Backend | deps/debt | M | MED | Live trigger DDL on the hottest tables; do with C6. |
| C9 | DEBT-01 consolidate the 12+ retry/backoff copies onto `_retry.py` + `scrapling_transport` defaults; migrate catalog_ingest linear→exponential first | Backend | debt | L | MED | Affects live scrape cadence; migrate one lane at a time behind characterization tests. |
| C10 | DEBT-05 extract the cookie loader/validator/registry cluster → `socials/cookie_store.py` (next god-module slice) | Backend | debt | M | MED | `globals()`-based cache clearing must be preserved exactly; characterization-test first. The highest-value extraction after 017. |
| C11 | DEBT-03 hoist `apply_decodo_session_affinity`/`cookies_to_scrapling` to the shared seam; stop SocialBlade importing Instagram internals | Backend | debt | S | LOW | Pure import repoint; pairs naturally with 028's proxy work. |
| C12 | SEC-04 prefer the authenticated principal over `X-TRR-Admin-User-*` headers for Reddit-admin audit actor | Backend | security | S | LOW | Confirm the upstream BFF boundary that sets those headers before inverting precedence. |
| C13 | PERF-02 drop the redundant TTL re-set + one of the deepcopies on cached week-detail hits | Backend | perf | S | LOW–MED | Must preserve the no-mutation-of-cache invariant; small, clean win. |
| C14 | BUG-05 decide default-on (with existing backoff/caps) vs. reflect failed followup in run status so `completed` isn't misleading | Backend | correctness | S | MED | Documented tradeoff — a product/ops decision, not a latent defect; scope as investigate. |
| C15 | DEPS-05 drop `curl_cffi` from the lean+vision Modal lanes | Backend | deps | S | LOW | Sole import is lazy+guarded; verify no lean-lane scheduler does live TikTok HTTP fetches. |
| C16 | DOC-01 add a "Social scraper runtime" section to `env-contract.md` (credential, proxy/Decodo, GraphQL doc-id override groups) | Both | docs | M | LOW | The grep inventory exists; needs curation (name credential *vars*, never values). |
| C17 | DOC-02 annotate/correct `.env.example` for injector-pinned vs. tunable social keys | Backend | docs | S | LOW | The doc half of 025; do right after 025 lands so the annotation matches the new tunable set. |
| C18 | TEST-01/02/06 promote a curated scrape/budget subset (comments fetcher retry, job-runner cancellation, control-plane budget, decodo accounting) into the blocking `tests/api` lane or a second required job | Backend | tests | M | LOW | The highest-churn engine is protected only by the non-blocking lane; 033 is the first instance. |
| C19 | TEST-05 wire a `RUN_DB_TESTS=1` CI job (ephemeral Postgres) so the dormant catalog-backfill integration test actually runs | Backend | tests | M–L | LOW/MED | Needs a DB service in CI; the persist step has no merge-blocking coverage. |
| C20 | DEBT-06/07 second god-module slice (media-mirror→S3 subsystem) + break up the 8,041-line "General helpers" band | Backend | debt | L | MED–HIGH | Do after C10 proves the extraction pattern; larger and more interleaved. |
| C21 | SEC-05 harden `_build_tiktok_sound_url` (drop absolute-URL passthrough / validate before `requests.get`) | Backend | security | S | LOW | Latent sink (currently reachable only via digit-normalized IDs); defense-in-depth. |
| C22 | SEC-06 `prepare_named_secrets.py` clean up rendered plaintext secret files in a `finally` (not only on `--apply`) | Backend | security | S | LOW | `.artifacts/` is gitignored; residual local-disk exposure only. |
| C23 | DEBT-08/09/10/11 modal_jobs importing `_probe_*` privates; barely-used `rollout_flags`; run/start/launch entrypoint naming; forked `cookies_to_scrapling` | Backend | debt | S–M | LOW | Facade/naming hygiene; lower leverage, promote opportunistically. |
| C24 | PERF-03/04/05/06 uncached `_relation_exists`; catalog retry-targets N+1 + `SELECT *`; app fallback landing full-scan; `p.*` JSONB over-fetch | Both | perf | S–M | LOW–MED | Real but lower-frequency (admin-triggered or fallback-only) than PERF-01; PERF-06 needs EXPLAIN first. |
| C25 | DEPS-04/06/07/08 unbatched rollup backfill on hot tables; verify Modal `asgi_app` kwargs vs 1.5 SDK; stale DeepFace comment in `requirements.txt`; migration-history-as-reconciliation | Backend | deps | S–M | LOW | Replay-only lock risk / network-gated checks / doc fixes; low urgency. |

Other confirmed-but-minor (not individually ranked): TEST-04 deferred-followup
blocking test mocks the state machine (shallow coverage); TEST-07 comments/catalog
mutation endpoints guarded only by the route-shape snapshot; TEST-08 the
browser/vision/live pytest markers are unused (the `-m "not …"` filter is a no-op);
BUG-03 `update_social_post` persists the un-normalized URL (`admin_social_posts.py:128,150`);
BUG-07 fragile `asyncio.get_event_loop()`+`nest_asyncio` in the Twitter scraper.

## Direction findings (options for the maintainer — spikes, not build-everything)

Grounded in repo evidence; strategy is yours. Selected ones become design/spike
plans. These are **new/refined** vs. the first run's D1–D4.

- **DIR-01 — Turn on the finished, dark Instagram-comments proxy fan-out.** The
  per-egress budgeted fan-out (`comments_scrapling/proxy_budget.py`,
  `public_mode.py`, wired in `job_runner.py:3861`, ledger migration
  `20260629140000`, tests present) is complete but inert: the enable flag and
  `$/GB` price are set **only in tests**, and price default `0.0` disables the
  kill-switch. *Impact*: activates a tested throughput upgrade on the pipeline's
  heaviest lane. *Trade-off*: Decodo bills by GB with no cross-run cap — a canary
  needs external spend monitoring. Effort S–M (it's a monitored rollout, not a
  build). **Strongest-grounded direction item this run.**

- **DIR-03 — Ship the built-but-dark Gemini sentiment upgrade.**
  `SOCIAL_SENTIMENT_GEMINI_ENABLED` (default False; set only in tests) gates an
  LLM sentiment path with model selection + caps already coded
  (`social_season_analytics_impl.py:52074,51931`); the consuming UI already ships
  on the rule-based output (`…/tiktok/sentiment-trends/route.ts`). *Impact*:
  upgrades a feature users already see. *Trade-off*: per-comment LLM cost/latency
  (120-comment cap bounds it); needs a labeled eval + cost ceiling before
  default-on. Effort M.

- **DIR-02 — Close the platform parity gap (comments + rollups beyond IG/TikTok/YT).**
  `pipelines/comments/` holds only `instagram.py`; rollup migrations exist for 3
  of 6 platforms; the team's own `docs/architecture/social-platform-module-checklist.md`
  blocks new comments lanes pending a documented "Comments Contract." *Impact*:
  uniform per-show analytics across platforms (the implicit promise). *Trade-off*:
  new worker lanes/tables — high blast radius by the checklist's own Stop Rules.
  Start with the TikTok Comments Contract (rollups already exist). Effort L.

- **DIR-04 — Decide: wire or delete the Instagram multi-runtime fallback.**
  `instagram/runtimes/` is ~800 lines of scaffold (Scrapling→Crawlee→crawl4ai→
  browser-use) that stays `unhealthy`/`RuntimeUnsupported` and is imported
  nowhere. *Impact*: a working fallback router is real anti-block resilience; as-is
  it's dead scaffold that reads as supported. *Trade-off*: L to finish (new
  external deps), S to delete. Spike the Scrapling→Crawlee route on profile-JSON,
  measure block-rate, then decide.

- **DIR-05 — Finish the stalled read-model extraction (enabler).**
  `read_models/coverage/` and `read_models/season_analytics/` are docstring-only
  packages that *declare* ownership while the reads stay in the 68.7K-line god
  file. *Impact*: the meta-blocker under parity/public-surface/testability work.
  *Trade-off*: L, incremental, HIGH risk — must be behind characterization tests.
  This is the strategic version of backlog C10/C20.

- **DIR-06 — Give the public social surface its own data/API layer.** The public
  `social/[platform]/[handle]/page.tsx` imports the **admin**
  `SocialAccountProfilePage` directly and every social API route lives under
  `app/api/admin/`. *Impact*: refines the first run's D1 — a public surface is
  half-built by borrowing admin internals, with no public auth/rate-limit/PII
  boundary. *Trade-off*: M–L; admin components may leak internal fields if exposed
  as-is. Spike a public read API over the DIR-05 read models + split the component.

## Findings considered and rejected this run (so they are not re-audited)

- **Week-window inclusive/exclusive split-brain** (2026-07-02 operator lead):
  REJECTED — the app week routes are pure proxies; `resolve_week_window`
  (`social_season_analytics_impl.py:52631`) is the single source of truth. No
  app-side window math.
- **Modal DB connection churn per job/task**: REJECTED — `db/pg.py` uses a
  module-level `ThreadedConnectionPool` reused via getconn/putconn; pools close
  only at worker end. Intentional session-pooler release, not churn.
- **Comment pagination infinite-loop / dedup gap**: REJECTED — `fetcher.py:4333`
  has repeated-cursor + `seen_cursors` no-progress guards; DOM dedup at `:885`.
- **Scrape-lane fan-out has serialized independent I/O waste**: REJECTED — the
  comment write path already batches (`_pg_upsert_many` via `execute_values`);
  the flag-gated fan-out code is clean.
- **Dynamic SQL injection in the analytics god module**: REJECTED — sampled
  systematically; SQL interpolates only platform-keyed allowlisted identifiers
  with parametrized values; savepoint names regex-sanitized.
- **`crawl4ai`/`browser-use` bloat the images**: REJECTED — correctly NOT locked;
  imported only inside guarded scaffold runtimes.
- **ADR 0001 is stale**: REJECTED — its lane-budget vocabulary
  (`normal/reduced/paused/identity_blocked`) is live in `control_plane/budget.py:14-19`,
  matching CONTEXT.md. Not drifted.

## 2026-07-08 `execute all` batch (GPT-5.5 HIGH via Codex CLI)

All nine plans 025–033 were executed by a GPT-5.5 executor (reasoning `high`)
dispatched through the Codex CLI in isolated git worktrees, one at a time, each
reviewed by the advisor (scope → full diff → test audit → independent re-run of
the done criteria) before the next dispatch. **All nine APPROVED.** Every branch
is one commit ahead of base `8ea7aa1a` and lives in a worktree under
`<scratch>/wt/advisor-NNN` — **nothing merged, pushed, or committed to the user's
branch; merging is the user's call.**

| Plan | Verdict | Branch | Commit |
|------|---------|--------|--------|
| 025 | APPROVE | advisor/025-modal-runtime-injector-operator-overrides | `e1fbce52` |
| 026 | APPROVE | advisor/026-avatar-mirror-ssrf-guard | `effa1641` |
| 027 | APPROVE | advisor/027-season-analytics-comment-rollups | `5da49073` |
| 028 | APPROVE | advisor/028-converge-decodo-posts-proxy-gating | `b2878ca2` |
| 029 | APPROVE | advisor/029-cookie-refresh-browser-leak | `d6ce021b` |
| 030 | APPROVE | advisor/030-inline-ingest-aggregate-failures | `5ebd2925` |
| 031 | APPROVE | advisor/031-redact-instagram-sessionid-logs | `8c2b2ca7` |
| 032 | APPROVE | advisor/032-socials-router-error-detail-leak | `d6ec83fe` |
| 033 | APPROVE | advisor/033-comments-recovery-endpoint-tests | `67951e9a` |

**Base-state caveat (matters for merge):** worktrees branched from the committed
head `8ea7aa1a`, so these diffs do NOT contain the uncommitted 001–024 local
work. Files overlapping that local work: `modal_jobs.py` (025 adds a tunable-key
set + edits the injector; 028 adds one dict key — disjoint regions) and
`tests/test_modal_jobs.py` (025 adds 4 tests). Expect a possible small merge
reconcile there; all other branches touch files with no local overlap.

**Plan reconciliations applied mid-batch (harness/verification fixes, not scope
changes):** plans 025 and 028 had their `import trr_backend.modal_jobs`
verification commands prefixed with `TRR_MODAL_RUNTIME_SCHEDULER_ENABLED=1` — the
module runs an import-time Modal-maintenance-owner guard that raises unless one
owner var is set (this fails even in the main tree; the original commands were a
recon over-extrapolation from `import api.main`, which does not load modal_jobs).
Plan 025's "Current state" note was also corrected to point at the committed
`_clear_modal_owner_env` fixture rather than plan 001's uncommitted conftest.

**Codex dispatch notes for future `execute` runs on this workspace** (all baked
into `<scratch>/dispatch.sh` and the reconciled plans):
- Disable two under-development Codex features or the headless run hangs at
  `turn.started` waiting for input that never comes:
  `--disable default_mode_request_user_input --disable chronicle`.
- Grant the git-metadata path as a sandbox writable-root or the executor cannot
  commit from a linked worktree (`index.lock: Operation not permitted`):
  `-c 'sandbox_workspace_write.writable_roots=["…/TRR-Backend/.git"]'`.
- Always pipe the plan on stdin (`< plan.md`); never leave stdin open, or
  `codex exec` blocks on "Reading additional input from stdin…".
- Worktrees need the venv symlinked in (`ln -s …/.venv <wt>/.venv`); no
  per-worktree `pip install` is needed because `pytest.ini` uses `pythonpath=.`
  (no editable install), so the symlinked interpreter imports the worktree's own
  source.

## 2026-07-08 recon spot-check

- Backend import gate re-verified: `.venv/bin/python -c "import api.main"` → ok.
- Social surface sizes and the injector dict were read live for every plan; all
  excerpts in plans 025–033 are from the advisor's own reads, not subagent
  reports.

---

# 2026-07-08 social deep audit #3 (workers, scrapers, everything)

Third `/improve-more` run (`deep`), scoped to **everything social**: the
operator/worker scripts fleet (`scripts/socials/**`, `scripts/workers/**`), the
Reddit lane end-to-end, the YouTube + SocialBlade lanes, the Twitter/Threads/
Facebook lanes, the app-side social UI, the socials core subpackages the first
two runs did not sweep (`media_url_safety`, `account_catalog`, `analytics`,
`api`, `ops`, `pipelines`, `read_models`, `platforms`/`source_scopes`), and
session/secret storage at rest. Seven read-only category subagents fanned out;
**every finding tabled or planned below was re-read in the live (dirty,
authoritative) source by the advisor** before it was written. Heads unchanged
since the first two runs (root `ba70326` now; Backend `8ea7aa1a`, App
`83778e5c`). Plans **035–041** were written from this run.

Explicitly NOT re-audited (covered by the prior two runs): `modal_jobs.py`,
`modal_dispatch.py`, `api/routers/socials/**`, the 68.7K-line
`social_season_analytics_impl.py`, `control_plane/{run_lifecycle,dispatch_runtime}.py`,
the instagram/tiktok scraper lanes, `browser_cookie_refresh.py` (except its
private-file helpers, reused by 041), `inline_ingest.py`, the crawlee error
taxonomy, `decodo_usage.py`, and `pipelines/comments/instagram.py`.

## ⚠️ Repo visibility (security plans)

All three repos are **public**. Plans **035** (media-URL SSRF), **040**
(destructive-op safety), and **041** (at-rest permissions) describe
vulnerability *locations and remediations* but contain **no secret values and no
runnable exploit strings** — same remediation-focused posture as the already
-published 002/003/026/028/031/032. Plan 041 explicitly confirms **nothing is
tracked in git** (session dirs + `keys/` are gitignored), so it is a host at-rest
gap, not a repo leak. Bounded exposure; before `git add`/push decide as before:
gitignore `/plans/`, accept the public descriptions, or move the security plans
somewhere VCS-ignored. Non-interactive run, so no `--issues` were created.

## Vetted findings (leverage-ranked; ✅ = turned into a plan this run)

| # | Finding | Cat | Impact | Effort | Risk | Conf | Evidence | Plan |
|---|---------|-----|--------|--------|------|------|----------|------|
| SEC-10 | `media_url_safety` treats `localhost`/`*.localhost` as reserved test hosts → skips BOTH allowlist and DNS/blocked-IP checks even under a non-empty platform allowlist | security | SSRF-to-loopback from a scraped media URL; weakens the exact guard plan 026 routes avatars through | M | MED | HIGH | `media_url_safety.py:24-25,88-90,160-167` | ✅ 035 |
| SEC-11 | Same guard fails **open** on an empty allowlist (`reddit` + any unlisted platform get no host restriction); no IPv4-mapped/NAT64 unwrap in `_is_blocked_ip` | security | new platform/typo silently disables host allowlisting; mapped-IPv6 literals evade the private check | M | MED | MED | `media_url_safety.py:70-72,93-103,156,161` | ✅ 035 |
| BUG-10 | Reddit worker loop has no try/except; `execute_refresh_run` re-raises every non-403 failure AFTER marking the run failed + releasing the claim; supervisor has no respawn | correctness | one bad subreddit / 429 / 5xx stops Reddit refreshes for ALL communities until a manual supervisor restart | S | LOW | HIGH | `reddit_refresh.py:4306-4345,4363-4382`; wrapper `reddit_refresh_worker.py:64-69`; supervisor `start_remote_job_workers.sh:82,191` | ✅ 036 |
| BUG-11 | Social worker pool has NO SIGTERM handler; children are bare `Popen` (no `start_new_session`); cleanup only in `finally`/`KeyboardInterrupt` | correctness | pool stop/deploy terminates level-2 without cleanup → grandchildren orphaned, keep claiming jobs + spending proxy budget; heartbeats go stale not stopped | M | MED | HIGH | `worker.py:707-741,786-833` (no `import signal`); `start_worker_pool.sh:110,120,125` | ✅ 037 |
| BUG-12 | Threads posts lane sets `auth_failed=bool(self._raw_cookies)` on ANY legacy-scraper exception; runner checks that branch before the retry branch → escalating cross-container auth cooldown | correctness | a transient transport error becomes a self-inflicted, escalating 60s→1h lockout for the handle | S | LOW | MED | `threads/posts_scrapling/fetcher.py:305-311` → `job_runner.py:474-488`; escalation `instagram/auth_cooldown.py:166+` | ✅ 038 |
| BUG-13 | YouTube `_fetch_comment_replies` reassigns the continuation token with no advance-check / visited-set / page cap (the parent `fetch_comments` loop has the guard) | correctness | infinite loop issuing rate-limited requests on a stuck token; hangs the worker on one comment | S | LOW | MED | `youtube/scraper.py:4278-4325` vs guarded `:3606-3613` | ✅ 039 |
| BUG-14 | Three `retire_*` scripts UPDATE `status='cancelled' where id = any(%s)` with no status predicate; preview + mutation are two separate reads | correctness | a worker transitioning a matched job between read and UPDATE gets its live/completed job clobbered to cancelled | S | LOW | MED | `retire_stale_instagram_media_mirror_failures.py:156-171,181-185`; +`retire_duplicate_*` | ✅ 040 |
| SEC-12 | `cleanup_youtube_false_positives.py` runs a hard `DELETE` by default; `--dry-run` is opt-in (inverts the fleet's `--apply` convention) | security | an operator who omits `--dry-run` irreversibly deletes rows | S | LOW | HIGH | `cleanup_youtube_false_positives.py:65-106` vs sibling `--apply` default | ✅ 040 |
| SEC-13 | Live IG session cookies (several files) + all 3 GCP service-account private keys are **0644 world-readable**; code intends 0600 (drift); dirs 0755 | security | any local user/daemon reads live scraper sessions + GCP keys; **nothing tracked in git** (host at-rest only) | S | LOW | HIGH | `ls -l data/social-browser-sessions/instagram`, `keys/`; intent `browser_cookie_refresh.py:440-460` | ✅ 041 |
| BUG-15 | SocialBlade re-runs the expensive credential login for legitimately complete 14/30/31-day histories, and marks the good payload failed if login can't complete | correctness | unnecessary Playwright login every refresh + false-failed payloads block persistence of valid growth data | S | MED | MED | `socialblade/scraper.py:757-759,897,1188-1205,824-825` | backlog C26 |
| BUG-16 | Twitter interaction persistence counts every non-upserted item as a "duplicate", feeding low-yield suppression that can permanently drop real repair candidates | correctness | genuinely-missing replies/quotes get suppressed and never backfilled | M | LOW | MED | `twitter/ops.py:922,1074`; `twitter/repair_planner.py:142-151` | backlog C27 |
| DEBT-10 | App-side god components: `SocialAccountProfilePage.tsx` 13.6K lines / 116 useState / 49 useEffect; `season-social-analytics-section.tsx` 8.9K lines | debt | dominant change-risk + review-cost sink on the social surface; strong existing runtime tests make incremental extraction safe | L | MED | HIGH | `SocialAccountProfilePage.tsx` (measured); `season-social-analytics-section.tsx` | backlog C28 |
| DEBT-11 | Unreachable direct-SQL landing fallback (`safeLoadSocialProgressSummaries` `else` branch, ~230-line UNION over 6 platform tables) + zero-caller `getSocialLandingPayload` wrapper | debt | biggest single "app must not read social SQL" contract violation, dead but one refactor from reachable | S | LOW | HIGH | `social-landing-repository.ts:922-1160,2755-2763,2822-2827` | backlog C29 |
| DEBT-12 | Dead `socialblade/business_api.py` (198 lines, zero non-test callers) with 2 latent bugs inside (unsorted daily points; `_first_number` can't parse `%`) | debt | reads as a live capability; wire-or-delete decision needed | S | LOW | HIGH | `socialblade/business_api.py:28,36-49,86-103,164,195` | backlog C30 |
| BUG-17 | Naive-datetime `.timestamp()` window filters in the YouTube + Twitter CLI scrapers filter in host-local time against UTC publish timestamps (prod is UTC; CLI/non-UTC hosts drift) | correctness | boundary-day videos/tweets wrongly included/excluded on a non-UTC host | S | LOW | HIGH(YT)/MED(TW) | `youtube/scraper.py:126,132`+`scripts/.../scrape.py:52`; `twitter/scraper.py:1740-1744` | backlog C31 |
| PERF-10 | Threads GraphQL scrape fires one extra per-post view-count GraphQL request (feed payload lacks `impression_count`, so it fires for ~every post) | perf | roughly doubles request count on the heaviest full-history backfills → latency + 429 exposure | M | MED | HIGH | `threads/scraper.py:576-607,1515-1533` | backlog C32 |
| TEST-10 | The two largest scrapers (Twitter 3.5K LOC, Facebook 2.2K LOC) have near-zero parser-level coverage; YouTube media-resolver/cipher + reply-termination also uncovered | tests | high-churn Meta/X markup drifts silently degrade engagement/media/completeness | L | LOW | HIGH | `tests/socials/facebook/*` (2 files, no scraper.py), `tests/socials/twitter/*` | backlog C33 |
| DEBT-13 | Platform enum drift: `SOCIAL_SUPPORTED_PLATFORMS` (7, incl reddit) vs `CATALOG_SUPPORTED_PLATFORMS` (6) vs media allowlist (6); duplicate `normalize_source_scope`; `imdb` maps to an unsupported platform | correctness/debt | callers get inconsistent "supported" answers; reddit inherits the SEC-11 fail-open | S | LOW | HIGH | `platforms.py:7-15,36,51-62,77-82`; `source_scopes.py:8,13-18`; `social_season_analytics_impl.py:320` | backlog C34 |
| DEBT-14 | Account/JSON helper drift across the scripts fleet: `_normalize_account`/`_normalize_handle` ≥8 copies with divergent `str` vs `str\|None` contracts; `_json_safe` ×4, `_metadata_dict` ×5 | debt | a normalization fix must touch 8 sites or silently diverge; callers handle "empty account" inconsistently | M | LOW | HIGH | `repair_instagram_account_health.py:47`, `instagram/media_mirror_recovery.py:43`, +6 | backlog C35 |
| PERF-11 | Reddit worker shares one global `_HTTP_CLIENT` (unlocked adaptive cooldown + OAuth token) across all thread pools; one thread's success-decay undoes another's 429 backoff | perf/correctness | the rate-limit protection is racy under the concurrency it runs in → more 429s (which per BUG-10 crash the worker) | M | MED | MED | `reddit_refresh.py:1051-1147,1212-1289,1500-1510,4117-4131` | backlog C36 |
| PERF-12 | Reddit exhaustive/max_coverage runs accumulate all rows + full raw payloads in memory (page cap 10,000) before a single merge | perf | ~10^5–10^6 post dicts + raw JSON retained per run in the long-lived worker | M | MED | MED | `reddit_refresh.py:43,1212-1289,877-907`; app default `discover/route.ts:996` | backlog C37 |
| SEC-14 | App renders scraped source-media URLs directly in raw `<img>/<video>` tags via `canonicalizeHostedMediaUrl` (no protocol/host allowlist), bypassing the hosted-media mirror | security | NOT XSS (src sink), but admin browser fetches attacker-influenceable CDN URLs → IP/referer leak; defeats the mirror | S | LOW | MED | `SocialAccountProfilePage.tsx:13455-13469`; `lib/hosted-media.ts:68-95` | backlog C38 |
| DX-10 | `workers/` trio drifted: `reddit_refresh_worker` has boot log + kill-switch + interrupt handling; `admin_operations_worker` + `google_news_worker` have none | dx | two long-running workers have no startup breadcrumb, clean-exit code, or off-switch (debugging needs code changes) | S | LOW | HIGH | `admin_operations_worker.py:41-57`, `google_news_worker.py:23-35` vs `reddit_refresh_worker.py:40-69` | backlog C39 |
| DEBT-15 | "Room" modules copy `_core.__dict__` into their globals + `_sync_core_overrides()` per call under blanket `# ruff: noqa: F821` | debt | disables static analysis, not thread-safe, unreadable without loading the god module — the concrete mechanism behind the god-module coupling | L | MED | HIGH | `analytics/read_models.py:26-40`; `pipelines/account_catalog/{launch,progress}.py`; `read_models/account_profile/common.py:32-64` | backlog C40 |
| BUG-18 | Facebook comment extraction sets `is_reply=True` but never records `parent_source_comment_id` (Threads does); `document_fetcher_factory` DI seam is dead-wired | bug/debt | FB reply comments are un-threadable; a DI seam looks configurable but is inert | M(threading)/S(DI) | LOW | HIGH | `facebook/scraper.py:2238-2248,326-334,357` vs `threads/scraper.py:2199-2201` | backlog C41 |
| DEBT-16 | YouTube player-response marker/JSON extractor + playlist-id/url helpers duplicated ×4 across `scraper.py`/`media_resolver.py`/`posts_catalog/catalog.py` | debt | four copies must track YouTube markup in lockstep; a fix to one silently leaves the others broken | S–M | LOW | HIGH | `media_resolver.py:15-20,68-116` vs `scraper.py:240-245,1759-1807,1063-1086` vs `catalog.py:58-78` | backlog C42 |
| BUG-19 | YouTube `_parse_video_renderer` view-count `runs` fallback is unreachable (default `"0"` makes the `if not view_text` always false); zeroes runs-based counts | bug | channel-page video renderers that express views via `runs` parse as 0 | S | LOW | MED | `youtube/scraper.py:1206-1211` | backlog C43 |
| BUG-20 | Account browser-session locks are process-local `threading.RLock`; if the session dir is shared across Modal containers, concurrent writes can corrupt a session file | correctness | interleaved cookie/state writes drop an account's auth (only if the dir is shared — needs deployment verification) | M | MED | LOW | `account_browser_sessions.py:25-26,111-118,289-315` | backlog C44 |
| PERF-13 | `analytics/read_models.get_analytics` loads the full window's posts+comments into memory (~6 passes) and ships uncapped `rows` with full text when `include_rows=True` | perf | unbounded first-render/export payload on high-volume seasons (verify overlap with landed 027 before acting) | M | LOW | MED | `analytics/read_models.py:279-289,927-947` | backlog C45 |
| DEBT-17 | Reddit HTTP retry logic duplicated + divergent: `RedditHttpClient.get_json` vs `_fetch_post_comments_tree` (comments path skips the adaptive cooldown) | debt | rate-limit behavior differs by path; a tuning fix must touch two places | S | LOW | HIGH | `reddit_refresh.py:1080-1147` vs `:1856-1930` | backlog C46 |
| DEBT-18 | Stray gitignored session-cookie files persist in the working tree (`socialblade_cookies.json` with `cf_clearance`+Stripe cookies; twitter/threads/facebook `*_cookies.json`; a `.bak`) — unreferenced by the loader | security(debt) | latent leak via `git add -f`/tarball/backup; not committed | S | LOW | HIGH | `scripts/socials/{socialblade,twitter,threads,facebook}/*_cookies.json`; `.../bravotv.cookies.json.bak` | backlog C47 (041 sweeps `.bak`; rest = operator cleanup) |
| TEST-11 | `cast-socialblade-comparison.tsx` (2K lines) has a 62-line component test; the reddit worker-loop/concurrency paths are untested | tests | interactive/error states + worker resilience regress silently | M | LOW | HIGH | `tests/cast-socialblade-comparison.test.tsx` (62 lines); `tests/repositories/test_reddit_refresh.py` | backlog C48 |
| BUG-21 | Twitter syndication summary sends a wall-clock `token` instead of the id-derived token; if X enforces it, the CLI replies/quotes path aborts + 3 server fallbacks silently no-op | bug | investigate (X historically lax on this token); needs a live probe before a blind fix | S | LOW | LOW | `twitter/scraper.py:917-918,657,934` | backlog C49 (investigate) |

## Ranked backlog additions (confirmed, not planned this run) — continue at 042+

Pick any to promote. Ordered by rough leverage. (Continues C1–C25 from the second run.)

| Rank | Finding | Repo | Cat | Effort | Risk | Why deferred |
|---|---|---|---|---|---|---|
| C26 | BUG-15 gate the SocialBlade `period`-regex login-retry on `history_source`/row-count so complete 14/30/31-day authenticated payloads aren't re-logged-in and false-failed | Backend | correctness | S | MED | The heuristic also guards genuinely-short table captures — the fix must not reopen that hole; needs the authenticated-source set mapped. |
| C27 | BUG-16 give the Twitter upsert path a distinct inserted/duplicate/error signal so `duplicate_count` reflects real dedupe and errors don't drive low-yield suppression | Backend | correctness | M | LOW | Needs the monolith `upsert_tweet` return-contract confirmed (does it ever return None for non-duplicate reasons?). |
| C28 | DEBT-10 extract each tab of `SocialAccountProfilePage.tsx`/`season-social-analytics-section.tsx` into its own component + data-hook behind the existing runtime tests | App | debt | L | MED | Highest change-cost sink on the surface; multi-PR, but the 14K-line runtime test is a strong gate. Do incrementally. |
| C29 | DEBT-11 delete the unreachable direct-SQL landing fallback + the zero-caller `getSocialLandingPayload` wrapper | App | debt | S | LOW | Clean deletion (traced every call site); make `adminContext` required. Good weak-executor task. |
| C30 | DEBT-12 wire-or-delete `socialblade/business_api.py` (if keeping: add to `_AUTHENTICATED_HISTORY_SOURCES`, sort daily points, strip `%`) | Backend | debt | S–M | LOW | A decision, not just a fix; nothing imports it today. |
| C31 | BUG-17 coerce naive `date_start`/`date_end` to UTC in the YouTube + Twitter scrape configs (mirror `_coerce_dt`) | Backend | correctness | S | LOW | Two small independent fixes; prod is UTC so impact is CLI/non-UTC-host only, but clean. |
| C32 | PERF-10 make Threads per-post view-count enrichment opt-in/capped (or batch it) instead of one GraphQL round-trip per post | Backend | perf | M | MED | Changes the metric-completeness contract (some posts land `views=0` until enriched); needs a product call. |
| C33 | TEST-10 fixture-driven characterization tests for the Twitter + Facebook parsers (engagement/media/completeness) before any parser refactor | Backend | tests | L | LOW | The prerequisite that makes every parser fix in those two files safe; additive. |
| C34 | DEBT-13 derive catalog/media/socialblade "supported platform" sets from one source of truth; decide reddit's status; collapse the duplicate `normalize_source_scope` | Backend | correctness/debt | S | LOW | Pairs with 035 (reddit's media-guard fail-open) — do together or right after. |
| C35 | DEBT-14 extract a shared `scripts/socials/_common.py` account-normalizer + `_json_safe`; pick one `str\|None` contract | Backend | debt | M | LOW | Mechanical; watch the one `str`-returning caller. Pairs with the retry-dedup theme. |
| C36 | PERF-11 lock the Reddit `_HTTP_CLIENT` cooldown/token state (or per-pool clients) + a shared token-bucket limiter | Backend | perf | M | MED | Do AFTER 036 (loop resilience) so the 429s it reduces don't crash the worker; serializing pacing may slow high-concurrency runs. |
| C37 | PERF-12 stream Reddit exhaustive-run pages to periodic batch upserts (comments already do at 2,000) instead of accumulating all rows + raw payloads | Backend | perf | M | MED | Changes the discovery→persist flow; bounded by page caps but the ceiling is very high. |
| C38 | SEC-14 prefer hosted/mirrored URLs in the app and gate source-URL fallback on an `https:`+known-CDN allowlist (or a same-origin media proxy) | App | security | S | LOW | Not XSS; the win is stopping IP/referer leak to scraped CDNs and honoring the mirror. |
| C39 | DX-10 extract the reddit-wrapper boot-log + interrupt/except + enable-flag scaffold into a shared helper for all three `workers/*` wrappers | Backend | dx | S | LOW | Pure operability; pairs with 037's signal work. |
| C40 | DEBT-15 as the god-module extraction proceeds, retire the `__dict__`-copy/`_sync_core_overrides` room-module scaffolding rather than carrying it forward | Backend | debt | L | MED | The concrete anti-pattern under the recorded god-module coupling (C10/C20/DIR-05); characterization-test first. |
| C41 | BUG-18 set `parent_source_comment_id` on Facebook reply comments (mirror Threads) + resolve the dead `document_fetcher_factory` DI seam | Backend | bug/debt | M/S | LOW | Two independent FB fixes; the threading one needs the parent fbid located in the SSR window. |
| C42 | DEBT-16 promote one canonical YouTube player-response marker/extractor + playlist-id/url helper (`youtube/_html_utils.py`) and import it everywhere | Backend | debt | S–M | LOW | Pure-function extract-and-share with existing scraper tests around it. |
| C43 | BUG-19 fix the YouTube `_parse_video_renderer` view-count `runs` fallback (drop the `"0"` default so the empty check falls through) | Backend | bug | S | LOW | One-line fix; add a `runs`-shaped renderer fixture. |
| C44 | BUG-20 if the session dir is shared across containers, add an OS-level file lock (`flock`) + atomic temp-then-`os.replace` writes to the session store | Backend | correctness | M | MED | LOW-confidence until deployment confirms the dir is shared; moot if per-container ephemeral. Verify first. |
| C45 | PERF-13 cap/paginate the `include_rows` payload in `analytics/read_models.get_analytics` and push aggregation into SQL | Backend | perf | M | LOW | Confirm this isn't already folded into landed 027 before acting. |
| C46 | DEBT-17 route Reddit comment fetching through `get_json` so there is one retry/backoff + adaptive-cooldown implementation | Backend | debt | S | LOW | Pairs with C36; unifies the two divergent Reddit HTTP paths. |
| C47 | DEBT-18 move all platform `*_cookies.json` session files out of the repo tree (env/secret store) + delete the stray `.bak`/socialblade files; rotate the exposed sessions | Backend | security | S | LOW | 041 sweeps perms + the `.bak`; this is the "get sessions out of the checkout" follow-up + rotation. |
| C48 | TEST-11 thicken `cast-socialblade-comparison.tsx` tests + add Reddit worker-loop/concurrency coverage | Both | tests | M | LOW | 036/039 add some; this closes the interactive-component + concurrency gaps. |
| C49 | BUG-21 (investigate) probe whether X enforces the syndication `token`; if so, derive it from the tweet id (id-based base-36) + add a fixture | Backend | bug | S | LOW | Needs a live probe; historically X accepted arbitrary tokens. Investigate, don't blind-fix. |

## Direction findings (this run) — options for the maintainer

Grounded in repo evidence; strategy is yours. New vs. the prior runs' DIR items.

- **DIR-10 — Decide wire-or-delete for the dead SocialBlade Business API adapter.**
  `socialblade/business_api.py` is a complete 198-line "official API" adapter
  (`scrape_socialblade_business_api`, env gate `SOCIALBLADE_API_ENABLED/TOKEN`)
  with zero non-test callers, and its `history_source: "business_api"` isn't in
  `service._AUTHENTICATED_HISTORY_SOURCES` so even if wired its snapshots would
  never be "fresh". *Impact*: either a paid, robust data source turned on, or
  198 lines of dead weight removed. *Trade-off*: wiring needs the freshness-source
  fix + two latent-bug fixes (unsorted points, `%` parsing); deleting is trivial.
  Effort S–M. **Strongest wire-or-delete decision this run.**

- **DIR-11 — Give the god-components a real per-tab architecture (App enabler).**
  The social surface is carried by two client god-components (13.6K + 8.9K lines,
  178 combined `useState`). *Impact*: this is the meta-blocker under every future
  social-UI change and the dominant review-cost sink. *Trade-off*: L, multi-PR,
  MED risk — but the existing 14K-line runtime test is an unusually strong
  extraction net. The app-side analogue of the backend's DIR-05 read-model
  extraction. Effort L (incremental).

- **DIR-12 — Converge the two SSRF guards onto one implementation.**
  `media_url_safety.py` (weaker, protects the scraper fetch sinks) and
  `s3_mirror._public_media_url_error` (stronger, `is_global`-based, protects the
  mirror) enforce different rules and drift. *Impact*: one guard means a fix
  protects every fetch sink at once. *Trade-off*: M; consolidating on the strict
  `is_global` check may reject a few carrier-NAT CDN hosts — verify against the
  platform CDN suffix set. Plan 035 hardens the weak guard; this is the follow-on
  merge. Effort M.

- **DIR-13 — Standardize the operator-script safety + worker-operability contract.**
  The scripts fleet has ≥3 destructive-op guard conventions (`--apply` default,
  `--dry-run` opt-in, `--execute`, no guard at all) and the `workers/` trio has
  drifted error-handling. *Impact*: a single documented "dry-run default + `--apply`,
  boot-log + kill-switch + interrupt handling" contract makes the whole fleet safe
  to run from memory. *Trade-off*: S–M mechanical, but touches many files. Plans
  040 + 037 fix the highest-risk instances; this is the fleet-wide sweep. Effort M.

## Findings considered and rejected this run (so they are not re-audited)

- **App public social pages leak admin data (DIR-06 escalation)**: REJECTED as a
  leak — `SocialAccountProfilePage` is `"use client"`, takes only
  `{platform,handle,activeTab}` props (no server-fetched data), uses
  `useAdminGuard`, and every fetch hits a `requireAdmin`-gated route.
  Unauthenticated visitors get an empty shell + 401s. Remains the DIR-06
  *direction* item only.
- **XSS via scraped captions/comments in the app**: REJECTED — no
  `dangerouslySetInnerHTML` in any social component; scraped text renders as
  React text nodes. (The media-URL src concern is the separate, LOW SEC-14.)
- **Reddit value-level SQL injection**: REJECTED — `_upsert_posts`/`_upsert_comments`
  build only column-name lists dynamically (gated by `_column_exists`); all
  values go through parameterized `execute_values`. (The interpolated
  window/container-key *literals* in `reddit_refresh.py:416-432` are internally
  generated, not user-reachable — noted as debt, not a live injection.)
- **Reddit admin routes lack server-side authz**: REJECTED — all handlers depend
  on `InternalAdminUser` → `require_internal_admin`; destructive community/thread
  ops are gated. (The spoofable `X-TRR-Admin-User-*` *actor* precedence is the
  already-recorded C12, not re-reported.)
- **Session/key artifacts committed to the public repo**: REJECTED — `git ls-files`
  returns nothing under `data/`, `keys/`, `.locks/`; all gitignored. SEC-13/041
  is a host at-rest gap, not a repo leak. Fixtures under `tests/fixtures/socials`
  hold only boolean presence flags, no cookie values.
- **Decodo proxy password logged/leaked**: REJECTED — embedded in the proxy URL
  passed to httpx `proxy=` (standard basic-auth convention); logging uses a
  host:port fingerprint only.

## 2026-07-08 recon spot-check (this run)

- Backend import gate re-verified: `.venv/bin/python -c "import api.main"` → ok.
- Every plan 035–041 excerpt was read live from the dirty working tree by the
  advisor (not from subagent reports); the SSRF, Reddit-loop, worker-SIGTERM,
  Threads-cooldown, YouTube-reply-loop, retire-race, and 0644-permission findings
  were each independently confirmed against the source and the on-disk `ls -l`.

---

# 2026-07-08 `execute 035-041` batch (GPT-5.5 HIGH via Codex CLI) — DONE & ARCHIVED

All seven social deep-audit #3 plans were executed by a GPT-5.5 executor
(reasoning `high`) dispatched through the Codex CLI (`codex exec`, codex-cli
0.144.0-alpha.2) in isolated git worktrees off committed base `8ea7aa1a`, one per
plan, each reviewed by the advisor (scope diff → full diff read → test audit →
independent re-run of the done criteria in the worktree) before verdict.
**All seven APPROVED.** The plan files are archived under `plans/archive/`; the
branches live in worktrees under the advisor scratchpad — **nothing merged,
pushed, or committed to the user's branch; merging is the user's call.**

| Plan | Verdict | Branch | Commit | Notes |
|------|---------|--------|--------|-------|
| 035 | APPROVE (1 rev) | advisor/035-media-url-safety-ssrf-hardening | `f4a87c76` | 9 tests (3+6 new). Revision: original plan listed `tests/socials/test_avatar_ssrf_guard.py` as a verify target, but that file is from the unmerged 026 work and absent at base — plan corrected, file dropped. Loopback + `*.localhost` now rejected; empty allowlist fails closed (both branches); reddit allowlist added; ipv4-mapped IPv6 unwrapped. |
| 036 | APPROVE | advisor/036-reddit-worker-loop-resilience | `fd07a1a2` | 47 tests (2 new). `raise_on_failure` flag (Modal path still raises); loop wraps + continues on failure; supervisor respawn loop with `rc==0` break + TERM/INT forwarding. Verified: rc≠0 respawns, rc==0 does not. |
| 037 | APPROVE | advisor/037-worker-pool-sigterm-graceful-shutdown | `7e5a9a28` | 32 tests (1 new). SIGTERM/SIGINT→KeyboardInterrupt→existing cleanup; children `start_new_session=True`; `_stop_process` group-kills with fallback; pool `stop_all` group-TERM with PID fallback. |
| 038 | APPROVE | advisor/038-threads-transient-error-auth-cooldown | `14ff1913` | 20 tests (2 new: transient→no-cooldown-retry AND genuine-auth→cooldown preserved). Source change is one line (`auth_failed=False`); job_runner correctly left untouched. |
| 039 | APPROVE | advisor/039-youtube-reply-loop-termination-guard | `a8bb0640` | 24 tests (1 new: stuck-token → exactly one fetch, terminates). seen-set + page cap + non-advance break, mirroring the parent loop. |
| 040 | APPROVE | advisor/040-harden-destructive-social-operator-scripts | `95a7d067` | 30 tests (new per-script). Each retire UPDATE guard matches its `_fetch_matches` selection status set exactly (stale→`failed`; both duplicate scripts→`queued/pending/retrying/running`); `cleanup_youtube` now dry-run-by-default + `--apply`. |
| 041 | APPROVE (1 rev) | advisor/041-session-key-permission-reconciliation | `acafaf33` | 35 tests (3 new). Revision: plan's "no central keys-dir resolver" fallback was mis-filed under STOP; corrected to an explicit proceed-path. `reconcile_private_paths` (idempotent, best-effort) wired once into `_storage_root()`, **session-root only**; `keys/` deferred (no central resolver). Two in-file ruff line-wraps were necessary (base had pre-existing E501). |

## Consolidated integration branch + conflict-free landing (verified)

The seven branches touch **disjoint files** (no two share a file), so they merge
without inter-branch conflict. A consolidated branch **`advisor/social-batch-035-041`**
(off `8ea7aa1a`, all seven `--no-ff` merged, 24 files, +560/−37) was built and
verified as the whole-batch proof that *the scraper worker works with every fix
applied*:

- `import api.main`, `import scripts.socials.worker`,
  `import scripts.workers.reddit_refresh_worker`, and
  `from trr_backend.socials.browser_cookie_refresh import reconcile_private_paths`
  all succeed.
- Full touched-area suite: **178 passed**
  (media_url_safety, reddit_refresh, social_worker, threads fetcher+job_runner,
  youtube scraper, all 4 retire/cleanup script tests, cookie_refresh_flows).
- `bash -n` clean on both `start_worker_pool.sh` and `start_remote_job_workers.sh`.

**Landing on the active backend branch (`codex/publish/trr-backend/20260705-174033`,
currently dirty with the integrated 025–034 work) is CONFLICT-FREE:**

- 035–040 touch 22 files that are all **clean** in the active working tree → land as-is.
- 041 is the only overlap (via the already-integrated 029 change to
  `browser_cookie_refresh.py`): its two "overlapping" hunks (@351, @639) are
  **byte-identical** ruff line-wraps that git auto-merges; its substantive
  addition (`reconcile_private_paths` + the `_storage_root` call + import) is
  additive to regions 029 does not touch; `account_browser_sessions.py` is clean;
  and its `test_cookie_refresh_flows.py` append (tail ~1200) is disjoint from the
  active tree's edit (~443). Verified by hunk-anchor comparison — **no manual
  conflict resolution required.**

To land (the user's call — the advisor does not merge into the user's branch):
`git merge --no-ff advisor/social-batch-035-041` from the active branch, or
cherry-pick the seven commits, then re-run the touched-area suite. Modal deploy
follow-through for the runtime-affecting worker changes (036/037) is still
pending as usual.

## Operator action still required (plan 041)

The exposed credentials sat 0644 world-readable and must be treated as burned:
re-authenticate the affected Instagram scraper sessions and regenerate/rotate the
three GCP service-account keys in `TRR-Backend/keys/`. The code change hardens
go-forward permissions and the on-disk `.bak` is swept, but rotation is a manual
step the executor cannot perform.

---

# 2026-07-08 `execute 042-047` batch (GPT-5.5 HIGH via Codex CLI) — DONE & ARCHIVED

Five confirmed backlog findings from the social deep-audit #3 were promoted to
plans (042–045, 047; **046 reserved**, see its row) and executed by a GPT-5.5
executor (reasoning `high`) via Codex in isolated worktrees off `8ea7aa1a`, each
advisor-reviewed (scope → full diff → test audit → independent re-run) before
verdict. **All five APPROVED, first pass, no revisions.** Plan files archived
under `plans/archive/`; branches in scratchpad worktrees — nothing merged to the
user's branch.

| Plan | Verdict | Commit | Notes |
|------|---------|--------|-------|
| 042 | APPROVE | `730480cd` | 21 tests. 1-line god-module touch (`include_inserted_flag=True`) + symmetric insert/duplicate/skip counting in both catalog loops. Test: 2 inserts / 4 dups / 51 skips → `unique_saved_delta=2`, `duplicate_count=4`, `skipped_count=51`. |
| 043 | APPROVE | `51732723` | 33 tests. Period-regex retry gated on `history_source not in _SOCIALBLADE_AUTHENTICATED_HISTORY_SOURCES`. Tests: auth-30d→no-retry, unauth-30d→retry, no-metrics→retry. |
| 044 | APPROVE | `56b51201` | 25 tests. `simpleText` truthy default removed → runs-form counts parse. The like-count parser at `youtube/scraper.py:4220` has the same latent pattern (flagged follow-up, out of scope). |
| 045 | APPROVE | `c2f713db` | 34 tests. YT `YouTubeScrapeConfig.__post_init__` coerces naive dates to UTC; Twitter `_scrape_syndication` reuses the existing `_window_bound_timestamp`. |
| 047 | APPROVE | `4a9afb35` | 47 tests. `_state_lock` guards token cache + both cooldown mutations; every `session.*`/`time.sleep` verified outside the lock (read-into-local-then-sleep for the decay). Token-bucket redesign deferred. |

## Consolidated branch + conflict-free landing (verified)

The five branches touch disjoint files except 044 and 045 both edit
`youtube/scraper.py` at **disjoint hunks** (view-count block vs `__post_init__`),
which auto-merge. Consolidated branch **`advisor/social-batch-042-047`** (off
`8ea7aa1a`, 11 files, +317/−31) verified: `import api.main` OK, **137
touched-area tests pass**.

**Landing on the active backend branch is CONFLICT-FREE:** 043/044/045/047's
files are all clean in the active tree; 042's only dirty-file overlap is the
single `_pg_upsert` line in `social_season_analytics_impl.py`, which is
**unmodified** in the active tree (present at line 29199 without the flag), so its
1-line addition applies as a clean 3-way merge despite the line-number shift.

To land (user's call): `git merge --no-ff advisor/social-batch-042-047` from the
active branch, then re-run the touched-area suite. Modal deploy follow-through
for 047 (worker-runtime) is pending as usual.

## New follow-up recorded this batch

- **C50** — `youtube/scraper.py:4220` like-count parser (`voteCount` →
  `.get("simpleText", "0")` truthy default) has the identical dead-`runs`-fallback
  bug plan 044 fixed for view counts. Trivial S/LOW; promote alongside a future
  YouTube pass. (Surfaced by the 044 executor; left out of scope correctly.)
