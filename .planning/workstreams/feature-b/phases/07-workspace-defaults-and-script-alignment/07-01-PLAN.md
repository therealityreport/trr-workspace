---
phase: 07-workspace-defaults-and-script-alignment
plan: 01
type: execute
wave: 1
depends_on:
  - 06-01-PLAN.md
files_modified:
  - Makefile
  - profiles/default.env
  - profiles/local-cloud.env
  - profiles/local-docker.env
  - profiles/local-lite.env
  - profiles/local-full.env
  - docs/workspace/dev-commands.md
  - docs/workspace/env-contract.md
  - scripts/dev-workspace.sh
  - scripts/down-screenalytics-infra.sh
  - scripts/status-workspace.sh
  - docs/ai/local-status/*.md
autonomous: true
requirements:
  - SCPT-01
  - SCPT-02
  - SCPT-03
user_setup: []
must_haves:
  truths:
    - Root workspace help and bootstrap surfaces describe the cloud-first path as the default and Docker-backed infra as opt-in fallback.
    - Screenalytics-specific local infra assumptions are isolated behind explicit fallback messaging instead of blocking unrelated backend/app work.
    - Shared profiles and generated env docs reinforce one canonical no-Docker preferred path.
  artifacts:
    - `Makefile` help and target comments match the Phase 6 contract.
    - Profile headers/comments make the canonical profile obvious and compatibility aliases explicit.
    - Script status/startup/shutdown output no longer frames Docker-backed Screenalytics infra as the normal baseline.
    - A continuity note records the Phase 7 default-alignment decisions for Phase 8 adoption work.
  key_links:
    - `make dev` and `make help` are the first developer-facing contract surfaces.
    - `profiles/default.env` is the canonical profile reference.
    - `scripts/dev-workspace.sh` and `scripts/down-screenalytics-infra.sh` are the main runtime surfaces for fallback boundary clarity.
---

<objective>
Align root workspace defaults, profiles, and shared diagnostics with the cloud-first contract frozen in Phase 6.

Purpose: After Phase 6 froze the policy, the repo still has residual wording and compatibility-profile drift that can nudge developers toward Docker-backed local infra. Phase 7 closes that gap so the default experience is visibly cloud-first while Docker remains available only as an explicit fallback.
Output: Updated root help/default surfaces, clarified profile intent, tighter Screenalytics fallback messaging, and continuity notes that leave no ambiguity about the preferred path.
</objective>

<context>
@.planning/PROJECT.md
@.planning/workstreams/feature-b/ROADMAP.md
@.planning/workstreams/feature-b/REQUIREMENTS.md
@.planning/workstreams/feature-b/STATE.md
@.planning/workstreams/feature-b/phases/07-workspace-defaults-and-script-alignment/07-CONTEXT.md
@.planning/workstreams/feature-b/phases/07-workspace-defaults-and-script-alignment/07-RESEARCH.md
@.planning/workstreams/feature-b/phases/07-workspace-defaults-and-script-alignment/07-VALIDATION.md
@Makefile
@profiles/default.env
@profiles/local-cloud.env
@profiles/local-docker.env
@profiles/local-lite.env
@profiles/local-full.env
@scripts/dev-workspace.sh
@scripts/down-screenalytics-infra.sh
@scripts/status-workspace.sh
@docs/workspace/dev-commands.md
@docs/workspace/env-contract.md
</context>

<tasks>

<task>
  <name>Task 1: Align root help and default-facing surfaces</name>
  <files>Makefile, docs/workspace/dev-commands.md</files>
  <action>Tighten root `Makefile` comments, help text, and any mirrored command docs so `make dev` is unmistakably the preferred cloud-first path and `make dev-local` is unmistakably explicit fallback. Preserve compatibility aliases, but label them as compatibility paths rather than alternate defaults.</action>
  <verify>bash /Users/thomashulihan/Projects/TRR/scripts/check-workspace-contract.sh</verify>
</task>

<task>
  <name>Task 2: Make shared profiles reflect one canonical path</name>
  <files>profiles/default.env, profiles/local-cloud.env, profiles/local-docker.env, profiles/local-lite.env, profiles/local-full.env, docs/workspace/env-contract.md</files>
  <action>Clarify which profile is canonical, which are compatibility aliases, and which are explicit fallback paths. Keep behavior stable where possible; Phase 7 is about alignment and clarity, not inventing new profile semantics.</action>
  <verify>python3 /Users/thomashulihan/Projects/TRR/scripts/env_contract_report.py validate</verify>
</task>

<task>
  <name>Task 3: Isolate Screenalytics local infra behind explicit fallback messaging</name>
  <files>scripts/dev-workspace.sh, scripts/down-screenalytics-infra.sh, scripts/status-workspace.sh, docs/ai/local-status/*.md</files>
  <action>Update startup, status, and shutdown output so local Redis + MinIO via Docker is clearly a special-case Screenalytics fallback and not a prerequisite for ordinary backend/app work. Record the resulting boundary in one Phase 7 continuity note.</action>
  <verify>bash -n /Users/thomashulihan/Projects/TRR/scripts/dev-workspace.sh /Users/thomashulihan/Projects/TRR/scripts/down-screenalytics-infra.sh /Users/thomashulihan/Projects/TRR/scripts/status-workspace.sh</verify>
</task>

</tasks>

<verification>
- [ ] `bash /Users/thomashulihan/Projects/TRR/scripts/check-workspace-contract.sh`
- [ ] `python3 /Users/thomashulihan/Projects/TRR/scripts/env_contract_report.py validate`
- [ ] `bash -n /Users/thomashulihan/Projects/TRR/scripts/dev-workspace.sh /Users/thomashulihan/Projects/TRR/scripts/down-screenalytics-infra.sh /Users/thomashulihan/Projects/TRR/scripts/preflight.sh /Users/thomashulihan/Projects/TRR/scripts/doctor.sh /Users/thomashulihan/Projects/TRR/scripts/status-workspace.sh`
- [ ] Manual review confirms the preferred/fallback contract is consistent across help text, profiles, and runtime messages
</verification>

<output>
After completion, create `.planning/workstreams/feature-b/phases/07-workspace-defaults-and-script-alignment/07-01-SUMMARY.md`
</output>
