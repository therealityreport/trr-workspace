# Env Contract Deprecation Report

Generated by `scripts/env_contract_report.py`. Do not edit by hand.

This report enumerates remaining deprecated-name occurrences across active repo files, compatibility shims, and archived/history surfaces.

## Summary

- `active drift`: 0
- `compatibility-only`: 80
- `historical/docs`: 140

## Active Drift

No occurrences in this category.

## Compatibility-Only

| Deprecated name | Location | Rationale |
|---|---|---|
| `DATABASE_URL` | `TRR-APP/apps/web/POSTGRES_SETUP.md:20:- `DATABASE_URL` is not part of the app runtime contract. The migration runner still accepts it as a compatibility-only input for older tooling flows, but it is no longer the preferred source.` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-APP/apps/web/scripts/check-survey-x.mjs:14:const connectionString = process.env.DATABASE_URL;` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-APP/apps/web/scripts/check-surveys.mjs:13:const connectionString = process.env.DATABASE_URL;` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-APP/apps/web/scripts/delete-web-scrape-imports.mjs:52:const connectionString = process.env.DATABASE_URL;` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-APP/apps/web/scripts/delete-web-scrape-imports.mjs:54:console.error("[delete-web-scrape-imports] DATABASE_URL is not set");` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-APP/apps/web/scripts/delete-web-scrape-imports.mjs:63:// Configure SSL (best-effort; DATABASE_URL often includes sslmode=require)` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-APP/apps/web/scripts/import-rhoslc-s6.mjs:6:*   DATABASE_URL="postgresql://..." node scripts/import-rhoslc-s6.mjs` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-APP/apps/web/scripts/import-rhoslc-s6.mjs:24:const connectionString = process.env.DATABASE_URL;` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-APP/apps/web/scripts/import-rhoslc-s6.mjs:26:console.error("[import] DATABASE_URL is not set");` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-APP/apps/web/scripts/run-migrations.mjs:137:["DATABASE_URL", process.env.DATABASE_URL],` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-APP/apps/web/scripts/run-migrations.mjs:143:"[migrations] No database URL is set. Configure TRR_DB_URL or optional TRR_DB_FALLBACK_URL. DATABASE_URL is compatibility-only for older tooling flows.",` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-APP/apps/web/scripts/run-migrations.mjs:192:if (connectionSource === "DATABASE_URL") {` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-APP/apps/web/scripts/run-migrations.mjs:194:"[migrations] Using DATABASE_URL compatibility input. Prefer TRR_DB_URL and optional TRR_DB_FALLBACK_URL for app runtime and migration tooling.",` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-APP/apps/web/src/lib/server/postgres.ts:165:"No database connection string is set. Configure TRR_DB_URL (recommended in TRR-APP/apps/web/.env.local for make dev) or TRR_DB_FALLBACK_URL. Runtime reads do not use SUPABASE_DB_URL or DATABASE_URL.",` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-APP/apps/web/tests/postgres-connection-string-resolution.test.ts:47:"No database connection string is set. Configure TRR_DB_URL (recommended in TRR-APP/apps/web/.env.local for make dev) or TRR_DB_FALLBACK_URL. Runtime reads do not use SUPABASE_DB_URL or DATABASE_URL.",` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-APP/scripts/auto-categorize-flairs.ts:8:* Requires env vars: TRR_DB_URL, optional TRR_DB_FALLBACK_URL, or DATABASE_URL for tooling-only flows` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-APP/scripts/auto-categorize-flairs.ts:75:const candidates = [process.env.TRR_DB_URL, process.env.TRR_DB_FALLBACK_URL, process.env.DATABASE_URL]` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-APP/scripts/auto-categorize-flairs.ts:80:console.error("No database connection string found. Set TRR_DB_URL, optional TRR_DB_FALLBACK_URL, or DATABASE_URL.");` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/docs/deploy/cloud_run.md:33:Set exactly one runtime DB URL in Cloud Run. The canonical runtime contract is `TRR_DB_URL`, and it should point at the Supavisor session pooler on `pooler.supabase.com:5432`. Keep `DATABASE_URL` toolchain-only for external tools that insist on it.` | The deprecated name is referenced only to document or enforce non-preferred fallback behavior. |
| `DATABASE_URL` | `TRR-Backend/scripts/README.md:37:- `DATABASE_URL` only when a standalone third-party tool requires that exact variable name` | The deprecated name is referenced only to document or enforce non-preferred fallback behavior. |
| `DATABASE_URL` | `TRR-Backend/scripts/_db_url.py:51:ordered_sources.append(("DATABASE_URL", _read_env("DATABASE_URL"), True))` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/scripts/_db_url.py:66:"and use DATABASE_URL only for tooling-specific flows."` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/scripts/db/README.md:36:3. **`SUPABASE_DB_URL` / `DATABASE_URL`** - Deprecated compatibility inputs only` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-Backend/scripts/db/README.md:253:| `DATABASE_URL` | Tooling-only Postgres connection | Third-party tools that explicitly require this name |` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-Backend/scripts/db/README.md:277:# Only for tooling that explicitly requires DATABASE_URL` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-Backend/scripts/db/README.md:278:export DATABASE_URL="$TRR_DB_URL"` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `DATABASE_URL` | `TRR-Backend/scripts/db/guard_core_schema.sql:12:--   cat scripts/db/guard_core_schema.sql your_script.sql | psql "${TRR_DB_URL:-${TRR_DB_FALLBACK_URL:-$DATABASE_URL}}"` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/scripts/db/run_sql.sh:15:#   DATABASE_URL        - Tooling-only compatibility input` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/scripts/db/run_sql.sh:56:if [[ -n "${DATABASE_URL:-}" ]]; then` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/scripts/db/run_sql.sh:57:echo "$DATABASE_URL"` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/scripts/db/run_sql.sh:89:elif [[ -n "${DATABASE_URL:-}" ]]; then` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/scripts/db/run_sql.sh:90:echo "DATABASE_URL"` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/scripts/db/run_sql.sh:125:echo "  DATABASE_URL        - Tooling-only compatibility input"` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/scripts/db/run_sql.sh:142:Or set DATABASE_URL only for tooling-specific local flows."` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/scripts/modal/prepare_named_secrets.py:21:RETIRED_DB_ENV_NAMES = ("SUPABASE_DB_URL", "DATABASE_URL")` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/scripts/ops/cast_screentime_stale_run_drill.py:23:for key in ("TRR_DB_URL", "TRR_DB_FALLBACK_URL", "DATABASE_URL", "SUPABASE_DB_URL"):` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/scripts/ops/cast_screentime_stale_run_drill.py:27:raise ValueError("TRR_DB_URL, optional TRR_DB_FALLBACK_URL, or DATABASE_URL is required")` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/scripts/supabase/generate_schema_docs.py:93:"(preferred: run `make schema-docs`). DATABASE_URL remains tooling-only."` | The deprecated name is referenced only to document or enforce non-preferred fallback behavior. |
| `DATABASE_URL` | `TRR-Backend/scripts/sync/sync_shows_all.py:65:'  psql "${TRR_DB_URL:-${TRR_DB_FALLBACK_URL:-$DATABASE_URL}}" -f scripts/db/reload_postgrest_schema.sql'` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/tests/db/test_connection_resolution.py:23:for name in ("TRR_DB_FALLBACK_URL", "TRR_DB_URL", "SUPABASE_DB_URL", "DATABASE_URL"):` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/tests/db/test_connection_resolution.py:107:"""Legacy runtime envs (SUPABASE_DB_URL, DATABASE_URL) and supabase status` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/tests/db/test_connection_resolution.py:128:"DATABASE_URL",` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/tests/scripts/test_prepare_named_secrets.py:83:"DATABASE_URL": "postgresql://legacy-tooling",` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/tests/scripts/test_prepare_named_secrets.py:90:assert "DATABASE_URL" not in result` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/trr_backend/db/postgrest_cache.py:124:'  psql "${TRR_DB_URL:-${TRR_DB_FALLBACK_URL:-$DATABASE_URL}}" '` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/trr_backend/db/preflight.py:122:Tooling may still accept DATABASE_URL for isolated migration or psql flows.` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/trr_backend/db/preflight.py:128:db_url = (os.getenv("TRR_DB_URL") or os.getenv("TRR_DB_FALLBACK_URL") or os.getenv("DATABASE_URL") or "").strip()` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/trr_backend/db/preflight.py:133:"or DATABASE_URL only for tool-specific migration flows."` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/trr_backend/repositories/cast_fandom.py:27:'  psql "${TRR_DB_URL:-${TRR_DB_FALLBACK_URL:-$DATABASE_URL}}" -f scripts/db/reload_postgrest_schema.sql'` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/trr_backend/repositories/cast_photos.py:36:'  psql "${TRR_DB_URL:-${TRR_DB_FALLBACK_URL:-$DATABASE_URL}}" -f scripts/db/reload_postgrest_schema.sql'` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/trr_backend/repositories/show_images.py:40:'  psql "${TRR_DB_URL:-${TRR_DB_FALLBACK_URL:-$DATABASE_URL}}" -f scripts/db/reload_postgrest_schema.sql'` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `TRR-Backend/trr_backend/repositories/shows.py:85:'  psql "${TRR_DB_URL:-${TRR_DB_FALLBACK_URL:-$DATABASE_URL}}" -f scripts/db/reload_postgrest_schema.sql'` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `scripts/lib/runtime-db-env.sh:62:[[ -n "${DATABASE_URL:-}" ]] && return 0` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `scripts/lib/runtime-db-env.sh:65:trr_read_env_file_value "$env_file" "DATABASE_URL" >/dev/null 2>&1 && return 0` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `DATABASE_URL` | `scripts/lib/runtime-db-env.sh:107:echo "[${prefix}] Legacy-only app DB envs found in ${app_env}. SUPABASE_DB_URL and DATABASE_URL no longer satisfy TRR-APP runtime startup." >&2` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-APP/apps/web/src/lib/server/postgres.ts:165:"No database connection string is set. Configure TRR_DB_URL (recommended in TRR-APP/apps/web/.env.local for make dev) or TRR_DB_FALLBACK_URL. Runtime reads do not use SUPABASE_DB_URL or DATABASE_URL.",` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-APP/apps/web/tests/postgres-connection-string-resolution.test.ts:47:"No database connection string is set. Configure TRR_DB_URL (recommended in TRR-APP/apps/web/.env.local for make dev) or TRR_DB_FALLBACK_URL. Runtime reads do not use SUPABASE_DB_URL or DATABASE_URL.",` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/scripts/_db_url.py:53:ordered_sources.append(("SUPABASE_DB_URL", _read_env("SUPABASE_DB_URL"), True))` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/scripts/db/README.md:36:3. **`SUPABASE_DB_URL` / `DATABASE_URL`** - Deprecated compatibility inputs only` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `SUPABASE_DB_URL` | `TRR-Backend/scripts/db/README.md:252:| `SUPABASE_DB_URL` | Deprecated compatibility alias | Transitional fallback only |` | Tooling-only script still requires DATABASE_URL at invocation time; this is not a runtime contract. |
| `SUPABASE_DB_URL` | `TRR-Backend/scripts/db/run_sql.sh:16:#   SUPABASE_DB_URL     - Deprecated compatibility input` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/scripts/db/run_sql.sh:62:if [[ -n "${SUPABASE_DB_URL:-}" ]]; then` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/scripts/db/run_sql.sh:63:echo "$SUPABASE_DB_URL"` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/scripts/db/run_sql.sh:91:elif [[ -n "${SUPABASE_DB_URL:-}" ]]; then` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/scripts/db/run_sql.sh:92:echo "SUPABASE_DB_URL"` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/scripts/db/run_sql.sh:126:echo "  SUPABASE_DB_URL     - Deprecated compatibility input"` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/scripts/modal/prepare_named_secrets.py:21:RETIRED_DB_ENV_NAMES = ("SUPABASE_DB_URL", "DATABASE_URL")` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/scripts/ops/cast_screentime_stale_run_drill.py:23:for key in ("TRR_DB_URL", "TRR_DB_FALLBACK_URL", "DATABASE_URL", "SUPABASE_DB_URL"):` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/scripts/verify/verify_media_unification.py:159:or (os.getenv("SUPABASE_DB_URL") or "").strip()` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/tests/db/test_connection_resolution.py:23:for name in ("TRR_DB_FALLBACK_URL", "TRR_DB_URL", "SUPABASE_DB_URL", "DATABASE_URL"):` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/tests/db/test_connection_resolution.py:107:"""Legacy runtime envs (SUPABASE_DB_URL, DATABASE_URL) and supabase status` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/tests/db/test_connection_resolution.py:116:"SUPABASE_DB_URL",` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/tests/scripts/test_prepare_named_secrets.py:82:"SUPABASE_DB_URL": "postgresql://legacy",` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/tests/scripts/test_prepare_named_secrets.py:89:assert "SUPABASE_DB_URL" not in result` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `TRR-Backend/tests/scripts/test_prepare_named_secrets.py:95:cli._apply_runtime_overrides({"SUPABASE_DB_URL": "postgresql://legacy"}, disabled=False)` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `scripts/lib/runtime-db-env.sh:61:[[ -n "${SUPABASE_DB_URL:-}" ]] && return 0` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `scripts/lib/runtime-db-env.sh:64:trr_read_env_file_value "$env_file" "SUPABASE_DB_URL" >/dev/null 2>&1 && return 0` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_DB_URL` | `scripts/lib/runtime-db-env.sh:107:echo "[${prefix}] Legacy-only app DB envs found in ${app_env}. SUPABASE_DB_URL and DATABASE_URL no longer satisfy TRR-APP runtime startup." >&2` | Explicit compatibility layer or regression coverage for deprecated env handling. |
| `SUPABASE_SERVICE_ROLE_KEY` | `TRR-APP/apps/web/README.md:66:canonical server-side Supabase auth inputs for TRR-APP. The app does not use legacy `SUPABASE_URL` or `SUPABASE_SERVICE_ROLE_KEY` for this surface.` | The deprecated name is referenced only to document or enforce non-preferred fallback behavior. |
| `SUPABASE_SERVICE_ROLE_KEY` | `TRR-APP/apps/web/tests/server-auth-adapter.test.ts:140:process.env.SUPABASE_SERVICE_ROLE_KEY = "legacy-service-role";` | Explicit compatibility layer or regression coverage for deprecated env handling. |

## Historical/Docs

| Deprecated name | Location | Rationale |
|---|---|---|
| `DATABASE_URL` | `TRR-APP/docs/cross-collab/TASK11/STATUS.md:286:- `/trr/staging/SUPABASE_DB_URL` now explicitly matches `/trr/staging/DATABASE_URL`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-APP/docs/cross-collab/TASK14/STATUS.md:32:- 2026-03-27: Removed runtime fallback support for `SUPABASE_DB_URL` / `DATABASE_URL` and removed `SUPABASE_SERVICE_ROLE_KEY` fallback from TRR admin helpers; focused Vitest coverage passed.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-APP/docs/cross-collab/TASK14/STATUS.md:33:- 2026-03-27: Updated live Vercel envs so `TRR_DB_URL` is present for preview and production, added preview `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and removed production `DATABASE_URL` plus `SUPABASE_SERVICE_ROLE_KEY`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-APP/docs/cross-collab/TASK14/STATUS.md:35:- 2026-03-30: Verified the active `trr-app` Vercel project still exposes `TRR_DB_URL`, `NEXT_PUBLIC_SUPABASE_*`, and `TRR_CORE_SUPABASE_*` in the env inventory; production no longer exposes runtime `DATABASE_URL` or `SUPABASE_SERVICE_ROLE_KEY`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-APP/docs/cross-collab/TASK18/STATUS.md:25:- 2026-03-30: Completed the explicit Vercel env review pass. `DATABASE_URL`, production `POSTGRES_*`, production `SUPABASE_*`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, and pull-only platform vars are now documented as retained, while `FIREBASE_SERVICE_ACCOUNT` is documented as canonical app-owned. `docs/workspace/vercel-env-review.md` is now the source of truth for this surface.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-APP/docs/cross-collab/TASK8/PLAN.md:16:- remove duplicate `DATABASE_URL` entry.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-APP/docs/cross-collab/TASK8/STATUS.md:25:- February 17, 2026: Fixed `SCREENALYTICS_API_URL` default and removed duplicate `DATABASE_URL` in `apps/web/.env.example`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-Backend/docs/cross-collab/TASK11/STATUS.md:863:- Staging `DATABASE_URL` pointed to Supabase pooler (`aws-1-us-east-1.pooler.supabase.com:6543`), while API/worker SG egress previously lacked `tcp/6543`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-Backend/docs/cross-collab/TASK11/STATUS.md:868:- Captured `/trr/staging/DATABASE_URL` and confirmed `/trr/staging/SUPABASE_DB_URL` was missing pre-change.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-Backend/docs/cross-collab/TASK11/STATUS.md:877:- `/trr/staging/SUPABASE_DB_URL` = same value as `/trr/staging/DATABASE_URL`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-Backend/docs/cross-collab/TASK14/STATUS.md:32:- 2026-03-27: Removed runtime fallback support for `SUPABASE_DB_URL` and `DATABASE_URL`; `pytest -q tests/db/test_connection_resolution.py` passed.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-Backend/docs/cross-collab/TASK19/PLAN.md:23:- Allow `DATABASE_URL` and `SUPABASE_DB_URL` only as explicit tooling or transitional compatibility inputs where still required.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-Backend/docs/cross-collab/TASK19/PLAN.md:46:- `DATABASE_URL` remains tooling-only.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-Backend/docs/cross-collab/TASK24/PLAN.md:65:| TRR-Backend | `trr_backend/db/preflight.py` | `tooling_only` | `TRR_DB_URL`, `TRR_DB_FALLBACK_URL`, `DATABASE_URL` | Mixed | Tooling | Correct / deprecated legacy gated | No code change; tooling-only `DATABASE_URL` support stays quarantined |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-Backend/docs/cross-collab/TASK24/PLAN.md:66:| TRR-Backend | `scripts/_db_url.py` | `tooling_only` | `TRR_DB_URL`, `TRR_DB_FALLBACK_URL`, `DATABASE_URL`, `SUPABASE_DB_URL` | Mixed | Tooling | Correct / deprecated legacy gated | No code change; explicit tooling compatibility remains quarantined |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-Backend/docs/cross-collab/TASK24/PLAN.md:80:| TRR-APP | `apps/web/scripts/auto-categorize-flairs.ts` | `tooling_only` | `TRR_DB_URL`, `TRR_DB_FALLBACK_URL`, `DATABASE_URL` | Mixed | Tooling | Correct / tooling-only | No code change; keep `DATABASE_URL` quarantined to tooling |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `TRR-Backend/docs/cross-collab/TASK24/PLAN.md:81:| TRR-APP | `apps/web/scripts/run-migrations.mjs` | `tooling_only` | `DATABASE_URL` | Mixed | Tooling | Correct / tooling-only | No code change; migration script remains explicitly `DATABASE_URL`-driven |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/ai/local-status/env-contract-migration-and-local-startup-recovery-2026-03-30.md:42:- `DATABASE_URL`, production `POSTGRES_*`, production `SUPABASE_*`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, and pull-only platform/build vars are documented as retained.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/ai/local-status/supabase-connection-and-env-standardization-2026-03-30.md:62:- Tooling-only `DATABASE_URL` consumers:` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/ai/local-status/supabase-connection-and-env-standardization-2026-03-30.md:66:- `docs/workspace/vercel-env-review.md` remains the source of truth for active `trr-app` retained `DATABASE_URL`, `POSTGRES_*`, and `SUPABASE_*` integration-managed values` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/ai/local-status/supavisor-session-mode-and-egress-reduction-rollout.md:120:- Connection candidate order remains `DATABASE_URL`, `SUPABASE_DB_URL`, `TRR_DB_URL`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/ai/local-status/supavisor-session-mode-and-egress-reduction-rollout.md:134:3. `DATABASE_URL`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-final.md:14:- Treat `DATABASE_URL`, deprecated `SUPABASE_DB_URL`, derived direct fallback, and `supabase status` discovery as tooling-only behavior, never persistent runtime behavior.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-final.md:60:| runtime `DATABASE_URL`                     | Removed from runtime resolution                  |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-final.md:183:- runtime `DATABASE_URL`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-final.md:209:- [ ] Keep `trr_backend/db/preflight.py` tooling-oriented: it may still allow `DATABASE_URL` for migrations or `psql` flows, but its wording must clearly say runtime paths do not use it.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-final.md:385:- runtime `DATABASE_URL`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-final.md:464:- runtime `DATABASE_URL`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-v2.md:12:- Treat `DATABASE_URL`, deprecated `SUPABASE_DB_URL`, derived direct fallback, and `supabase status` discovery as tooling-only behavior, never persistent runtime behavior.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-v2.md:36:- Remove runtime support for `SUPABASE_DB_URL`, runtime `DATABASE_URL`, derived direct-host fallback, and local `supabase status` discovery from backend service resolution.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-v2.md:43:- Keep `trr_backend/db/preflight.py` tooling-oriented: it may still allow `DATABASE_URL` for migrations or `psql` flows, but its wording must clearly say runtime paths do not use it.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-v2.md:91:- Keep `DATABASE_URL` as a tooling adapter only for scripts that require that exact name, and document `DATABASE_URL="$TRR_DB_URL"` instead of treating it as runtime input.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-v2.md:126:- Remove active documentation that still treats `SUPABASE_DB_URL`, runtime `DATABASE_URL`, or `:6543` as a normal persistent-service path.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-v2.md:135:- Tooling-only compatibility surface: `DATABASE_URL`, deprecated `SUPABASE_DB_URL`, local `supabase status`, and optional `TRR_DB_ENABLE_DIRECT_FALLBACK`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-v2.md:162:| `DATABASE_URL` | ~~Runtime~~ | **Removed from runtime** (tooling-only) |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening.md:20:- Runtime code no longer accepts `SUPABASE_DB_URL` or `DATABASE_URL` as startup candidates.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening.md:31:- No repo-tracked runtime path needs legacy `SUPABASE_DB_URL` or `DATABASE_URL`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening.md:62:- `DATABASE_URL`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening.md:109:- `DATABASE_URL`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening.md:142:rg -n "SUPABASE_DB_URL|DATABASE_URL" TRR-Backend/trr_backend TRR-APP/apps/web/src screenalytics/apps/api screenalytics/tools` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:41:| `misconfigured` | Live/runtime code can silently select `:6543`, `db.<project>.supabase.co`, `SUPABASE_DB_URL`, or `DATABASE_URL` |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:59:| 7 | `scripts/_db_url.py` | tooling_only | `TRR_DB_URL`, `TRR_DB_FALLBACK_URL`, `DATABASE_URL` (opt-in), `SUPABASE_DB_URL` (opt-in) | Tooling | **Correct** | None — legacy aliases explicitly gated behind `allow_*` flags, not default |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:60:| 8 | `trr_backend/db/preflight.py` | tooling_only | `TRR_DB_URL`, `TRR_DB_FALLBACK_URL`, `DATABASE_URL` | Tooling | **Correct** | None — migration safety checks, `DATABASE_URL` accepted only as tooling fallback |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:65:**Legacy env vars in runtime code:** Zero. `SUPABASE_DB_URL` and `DATABASE_URL` are explicitly tested-and-rejected in `test_connection_resolution.py`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:83:**Legacy env vars:** Zero instances of `SUPABASE_DB_URL`, `DATABASE_URL`, `:6543`, or direct-host patterns anywhere in the repo.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:98:| 6 | `.env.local` entries | dead_code | `SUPABASE_DB_URL`, `DATABASE_URL` | Dead | **Dead** — rejected by resolver | Remove from `.env.local` (local file, not tracked) |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:100:**Legacy env vars in runtime code:** Zero. `SUPABASE_DB_URL` and `DATABASE_URL` are explicitly rejected by resolver with actionable error message.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:128:Remove `SUPABASE_DB_URL` and `DATABASE_URL` from `TRR-APP/apps/web/.env.local`. These are rejected by the resolver but still present with credentials. Not a tracked file — operator cleanup.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:165:| `DATABASE_URL` | Backend (`scripts/` only) | Tooling helper fallback, explicitly gated behind `allow_database_url=True` |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:174:| `DATABASE_URL` in `.env.local` | TRR-APP local file (not tracked) | Ignored by resolver. Remove from local file. |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:194:3. **`.env.local` dead entries.** `SUPABASE_DB_URL` and `DATABASE_URL` in TRR-APP `.env.local` are rejected by resolver but still present with credentials. Operator cleanup — not tracked by git.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:204:- **Intentionally quarantined:** `DATABASE_URL` and `SUPABASE_DB_URL` accepted only in tooling scripts behind explicit opt-in flags. `SUPABASE_SERVICE_ROLE_KEY` in ops scripts only.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:215:- **To change:** Ensure deploy docs classify `TRR_CORE_SUPABASE_*` as required server-auth envs (not dormant). Remove dead `SUPABASE_DB_URL`/`DATABASE_URL` from `.env.local`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:39:- `scripts/_db_url.py` — allows `DATABASE_URL` and deprecated `SUPABASE_DB_URL` behind explicit opt-in flags. Correct for tooling.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:40:- `trr_backend/db/preflight.py` — migration safety checks, accepts `DATABASE_URL` as tooling fallback. Correct.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:43:**Legacy env vars in runtime code:** Zero. `SUPABASE_DB_URL` and `DATABASE_URL` are explicitly tested-and-rejected.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:58:**Legacy env vars:** Zero instances of `SUPABASE_DB_URL`, `DATABASE_URL`, `:6543`, or direct-host patterns in runtime or tooling code.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:73:**Legacy env vars in runtime code:** Zero. `SUPABASE_DB_URL` and `DATABASE_URL` are explicitly rejected by resolver with error message.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:75:**`.env.local` dead entries:** `SUPABASE_DB_URL` and `DATABASE_URL` are set in `.env.local` but rejected by resolver. Harmless but confusing — should be removed from local file.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:107:| `DATABASE_URL` | Backend (scripts only) | Tooling helper fallback, explicitly gated |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:116:| `DATABASE_URL` in `.env.local` | TRR-APP local file | Ignored by resolver. Should remove from local file. |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:126:3. **`.env.local` dead entries.** `SUPABASE_DB_URL` and `DATABASE_URL` in TRR-APP's `.env.local` are rejected by the resolver but still present with credentials. Operator cleanup — remove them from the local file.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:149:- **Was wrong:** Nothing in code. `.env.local` has dead `SUPABASE_DB_URL`/`DATABASE_URL` entries (harmless, local file only)` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `DATABASE_URL` | `docs/superpowers/plans/2026-04-02-supabase-hardening-final-closure.md:16:- **Why:** Plan docs reference deprecated env names (`SUPABASE_DB_URL`, `DATABASE_URL`) historically; the classifier didn't recognize `docs/superpowers/plans/` as a historical path` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-APP/docs/cross-collab/TASK11/OTHER_PROJECTS.md:87:- Backend applied staging runtime fixes (SG `tcp/6543` egress + explicit `SUPABASE_DB_URL`) and reran resilience scripts.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-APP/docs/cross-collab/TASK11/STATUS.md:286:- `/trr/staging/SUPABASE_DB_URL` now explicitly matches `/trr/staging/DATABASE_URL`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-APP/docs/cross-collab/TASK14/STATUS.md:32:- 2026-03-27: Removed runtime fallback support for `SUPABASE_DB_URL` / `DATABASE_URL` and removed `SUPABASE_SERVICE_ROLE_KEY` fallback from TRR admin helpers; focused Vitest coverage passed.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/cross-collab/TASK11/OTHER_PROJECTS.md:170:- Backend staging infra/runtime corrections were applied (`tcp/6543` egress on API/worker SG + explicit `/trr/staging/SUPABASE_DB_URL`).` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/cross-collab/TASK11/STATUS.md:868:- Captured `/trr/staging/DATABASE_URL` and confirmed `/trr/staging/SUPABASE_DB_URL` was missing pre-change.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/cross-collab/TASK11/STATUS.md:877:- `/trr/staging/SUPABASE_DB_URL` = same value as `/trr/staging/DATABASE_URL`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/cross-collab/TASK11/STATUS.md:1142:- no local database migration was needed during this pass; local backend/dev execution was already pointed at Supabase via `SUPABASE_DB_URL`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/cross-collab/TASK14/STATUS.md:32:- 2026-03-27: Removed runtime fallback support for `SUPABASE_DB_URL` and `DATABASE_URL`; `pytest -q tests/db/test_connection_resolution.py` passed.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/cross-collab/TASK19/PLAN.md:23:- Allow `DATABASE_URL` and `SUPABASE_DB_URL` only as explicit tooling or transitional compatibility inputs where still required.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/cross-collab/TASK19/PLAN.md:45:- `SUPABASE_DB_URL` is deprecated and never the preferred runtime input.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/cross-collab/TASK19/STATUS.md:22:- 2026-03-30: Updated repository/runtime recovery hints to stop advertising `SUPABASE_DB_URL` as the normal runtime contract.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/cross-collab/TASK24/PLAN.md:66:| TRR-Backend | `scripts/_db_url.py` | `tooling_only` | `TRR_DB_URL`, `TRR_DB_FALLBACK_URL`, `DATABASE_URL`, `SUPABASE_DB_URL` | Mixed | Tooling | Correct / deprecated legacy gated | No code change; explicit tooling compatibility remains quarantined |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/cross-collab/TASK5/PLAN.md:86:- `SUPABASE_DB_URL` is a deprecated alias during transition` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/plans/2026-01-28-surveys-supabase-auth.md:72:Run: `psql "$SUPABASE_DB_URL" -c "\di surveys.survey_responses_one_per_user_per_survey"`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/plans/2026-01-28-surveys-supabase-auth.md:186:Run: `psql "$SUPABASE_DB_URL" -c "\df surveys.submit_response"`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/plans/2026-01-28-surveys-supabase-auth.md:210:-- Run with: psql "$SUPABASE_DB_URL" -f tests/db/test_survey_submit_rpc.sql` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/plans/2026-01-28-surveys-supabase-auth.md:272:Run: `psql "$SUPABASE_DB_URL" -f tests/db/test_survey_submit_rpc.sql`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `TRR-Backend/docs/plans/2026-02-12-show-icons.md:166:Run: `cd /Users/thomashulihan/Projects/TRR/TRR-Backend && psql "$SUPABASE_DB_URL" -f trr_backend/migrations/add_show_icons_table.sql`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/ai/local-status/shared-account-catalog-backfill-and-profile-ui.md:135:- The full local migration stream was pushed into that preview branch with `supabase db push --db-url <branch-db-url> --include-all`, and `make schema-docs-check` then passed cleanly with `SUPABASE_DB_URL` pointed at the branch.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/ai/local-status/supabase-read-path-stabilization-and-flashback-rpcs.md:19:- Updated [`reload_postgrest_schema.sh`](/Users/thomashulihan/Projects/TRR/TRR-Backend/scripts/reload_postgrest_schema.sh) to use the canonical runtime DB envs instead of legacy `SUPABASE_DB_URL`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/ai/local-status/supavisor-session-mode-and-egress-reduction-rollout.md:120:- Connection candidate order remains `DATABASE_URL`, `SUPABASE_DB_URL`, `TRR_DB_URL`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/ai/local-status/supavisor-session-mode-and-egress-reduction-rollout.md:132:1. `SUPABASE_DB_URL`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/ai/local-status/supavisor-session-mode-and-egress-reduction-rollout.md:151:- `winner_source=SUPABASE_DB_URL`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/ai/local-status/supavisor-session-mode-and-egress-reduction-rollout.md:157:- `[db-pool] init_attempt=0 source=SUPABASE_DB_URL host_class=pooler host=aws-1-us-east-1.pooler.supabase.com port=5432`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/ai/local-status/supavisor-session-mode-and-egress-reduction-rollout.md:229:- stale deployment before fix: `v100`, commit metadata `417aed7*`, but container inspection showed it was missing local uncommitted transaction/cache hardening and still had `SUPABASE_DB_URL=...pooler.supabase.com:6543`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/ai/local-status/supavisor-session-mode-and-egress-reduction-rollout.md:233:- fresh container env now has `SUPABASE_DB_URL=...pooler.supabase.com:5432`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/ai/local-status/supavisor-session-mode-and-egress-reduction-rollout.md:241:- fresh container env inspection still showed `SUPABASE_DB_URL=...pooler.supabase.com:5432`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/ai/local-status/supavisor-session-mode-and-egress-reduction-rollout.md:390:- repeated `[db-pool] init_attempt=0 source=SUPABASE_DB_URL host_class=pooler host=aws-1-us-east-1.pooler.supabase.com port=5432`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/ai/local-status/supavisor-session-mode-and-egress-reduction-rollout.md:503:- repeated `[db-pool] init_failed source=SUPABASE_DB_URL host_class=pooler host=aws-1-us-east-1.pooler.supabase.com port=5432 error=OperationalError`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/plans/2026-03-22-deepface-integration-plan.md:136:DEEPFACE_DB_URL=${SUPABASE_DB_URL}` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-final.md:14:- Treat `DATABASE_URL`, deprecated `SUPABASE_DB_URL`, derived direct fallback, and `supabase status` discovery as tooling-only behavior, never persistent runtime behavior.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-final.md:59:| `SUPABASE_DB_URL`                          | Removed from runtime resolution                  |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-final.md:182:- `SUPABASE_DB_URL`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-final.md:352:- [ ] Remove remaining deprecated alias handling from tooling-only code, especially `scripts/migrate_legacy_db_to_supabase.py` (`TRR_DB_URL_ALIAS = "SUPABASE_DB_URL"`).` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-final.md:463:- `SUPABASE_DB_URL`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-v2.md:12:- Treat `DATABASE_URL`, deprecated `SUPABASE_DB_URL`, derived direct fallback, and `supabase status` discovery as tooling-only behavior, never persistent runtime behavior.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-v2.md:36:- Remove runtime support for `SUPABASE_DB_URL`, runtime `DATABASE_URL`, derived direct-host fallback, and local `supabase status` discovery from backend service resolution.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-v2.md:126:- Remove active documentation that still treats `SUPABASE_DB_URL`, runtime `DATABASE_URL`, or `:6543` as a normal persistent-service path.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-v2.md:135:- Tooling-only compatibility surface: `DATABASE_URL`, deprecated `SUPABASE_DB_URL`, local `supabase status`, and optional `TRR_DB_ENABLE_DIRECT_FALLBACK`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening-v2.md:161:| `SUPABASE_DB_URL` | ~~Runtime~~ | **Removed from runtime** (tooling-only) |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening.md:20:- Runtime code no longer accepts `SUPABASE_DB_URL` or `DATABASE_URL` as startup candidates.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening.md:31:- No repo-tracked runtime path needs legacy `SUPABASE_DB_URL` or `DATABASE_URL`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening.md:63:- `SUPABASE_DB_URL`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening.md:110:- `SUPABASE_DB_URL`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-01-supabase-unified-hardening.md:142:rg -n "SUPABASE_DB_URL|DATABASE_URL" TRR-Backend/trr_backend TRR-APP/apps/web/src screenalytics/apps/api screenalytics/tools` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:41:| `misconfigured` | Live/runtime code can silently select `:6543`, `db.<project>.supabase.co`, `SUPABASE_DB_URL`, or `DATABASE_URL` |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:59:| 7 | `scripts/_db_url.py` | tooling_only | `TRR_DB_URL`, `TRR_DB_FALLBACK_URL`, `DATABASE_URL` (opt-in), `SUPABASE_DB_URL` (opt-in) | Tooling | **Correct** | None — legacy aliases explicitly gated behind `allow_*` flags, not default |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:65:**Legacy env vars in runtime code:** Zero. `SUPABASE_DB_URL` and `DATABASE_URL` are explicitly tested-and-rejected in `test_connection_resolution.py`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:83:**Legacy env vars:** Zero instances of `SUPABASE_DB_URL`, `DATABASE_URL`, `:6543`, or direct-host patterns anywhere in the repo.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:98:| 6 | `.env.local` entries | dead_code | `SUPABASE_DB_URL`, `DATABASE_URL` | Dead | **Dead** — rejected by resolver | Remove from `.env.local` (local file, not tracked) |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:100:**Legacy env vars in runtime code:** Zero. `SUPABASE_DB_URL` and `DATABASE_URL` are explicitly rejected by resolver with actionable error message.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:128:Remove `SUPABASE_DB_URL` and `DATABASE_URL` from `TRR-APP/apps/web/.env.local`. These are rejected by the resolver but still present with credentials. Not a tracked file — operator cleanup.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:166:| `SUPABASE_DB_URL` | Backend (`scripts/` only) | Deprecated tooling alias, explicitly gated behind `allow_deprecated_supabase_db_url=True` |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:173:| `SUPABASE_DB_URL` in `.env.local` | TRR-APP local file (not tracked) | Ignored by resolver. Remove from local file. |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:194:3. **`.env.local` dead entries.** `SUPABASE_DB_URL` and `DATABASE_URL` in TRR-APP `.env.local` are rejected by resolver but still present with credentials. Operator cleanup — not tracked by git.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:204:- **Intentionally quarantined:** `DATABASE_URL` and `SUPABASE_DB_URL` accepted only in tooling scripts behind explicit opt-in flags. `SUPABASE_SERVICE_ROLE_KEY` in ops scripts only.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:215:- **To change:** Ensure deploy docs classify `TRR_CORE_SUPABASE_*` as required server-auth envs (not dormant). Remove dead `SUPABASE_DB_URL`/`DATABASE_URL` from `.env.local`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:39:- `scripts/_db_url.py` — allows `DATABASE_URL` and deprecated `SUPABASE_DB_URL` behind explicit opt-in flags. Correct for tooling.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:43:**Legacy env vars in runtime code:** Zero. `SUPABASE_DB_URL` and `DATABASE_URL` are explicitly tested-and-rejected.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:58:**Legacy env vars:** Zero instances of `SUPABASE_DB_URL`, `DATABASE_URL`, `:6543`, or direct-host patterns in runtime or tooling code.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:73:**Legacy env vars in runtime code:** Zero. `SUPABASE_DB_URL` and `DATABASE_URL` are explicitly rejected by resolver with error message.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:75:**`.env.local` dead entries:** `SUPABASE_DB_URL` and `DATABASE_URL` are set in `.env.local` but rejected by resolver. Harmless but confusing — should be removed from local file.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:108:| `SUPABASE_DB_URL` | Backend (scripts only) | Deprecated tooling alias, explicitly gated |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:115:| `SUPABASE_DB_URL` in `.env.local` | TRR-APP local file | Ignored by resolver. Should remove from local file. |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:126:3. **`.env.local` dead entries.** `SUPABASE_DB_URL` and `DATABASE_URL` in TRR-APP's `.env.local` are rejected by the resolver but still present with credentials. Operator cleanup — remove them from the local file.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:149:- **Was wrong:** Nothing in code. `.env.local` has dead `SUPABASE_DB_URL`/`DATABASE_URL` entries (harmless, local file only)` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_DB_URL` | `docs/superpowers/plans/2026-04-02-supabase-hardening-final-closure.md:16:- **Why:** Plan docs reference deprecated env names (`SUPABASE_DB_URL`, `DATABASE_URL`) historically; the classifier didn't recognize `docs/superpowers/plans/` as a historical path` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_SERVICE_ROLE_KEY` | `TRR-APP/docs/cross-collab/TASK14/STATUS.md:32:- 2026-03-27: Removed runtime fallback support for `SUPABASE_DB_URL` / `DATABASE_URL` and removed `SUPABASE_SERVICE_ROLE_KEY` fallback from TRR admin helpers; focused Vitest coverage passed.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_SERVICE_ROLE_KEY` | `TRR-APP/docs/cross-collab/TASK14/STATUS.md:33:- 2026-03-27: Updated live Vercel envs so `TRR_DB_URL` is present for preview and production, added preview `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and removed production `DATABASE_URL` plus `SUPABASE_SERVICE_ROLE_KEY`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_SERVICE_ROLE_KEY` | `TRR-APP/docs/cross-collab/TASK14/STATUS.md:35:- 2026-03-30: Verified the active `trr-app` Vercel project still exposes `TRR_DB_URL`, `NEXT_PUBLIC_SUPABASE_*`, and `TRR_CORE_SUPABASE_*` in the env inventory; production no longer exposes runtime `DATABASE_URL` or `SUPABASE_SERVICE_ROLE_KEY`.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_SERVICE_ROLE_KEY` | `docs/ai/local-status/supabase-connection-and-env-standardization-2026-03-30.md:24:- backend Supabase API/auth: `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_JWT_SECRET`` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_SERVICE_ROLE_KEY` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:167:| `SUPABASE_SERVICE_ROLE_KEY` | Backend (`scripts/ops/` only) | Ops smoke tests, not runtime API |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_SERVICE_ROLE_KEY` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final-v2.md:204:- **Intentionally quarantined:** `DATABASE_URL` and `SUPABASE_DB_URL` accepted only in tooling scripts behind explicit opt-in flags. `SUPABASE_SERVICE_ROLE_KEY` in ops scripts only.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_SERVICE_ROLE_KEY` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:41:- `SUPABASE_SERVICE_ROLE_KEY` — referenced only in `scripts/ops/` smoke tests and `.env.example`. Not used by runtime API.` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_SERVICE_ROLE_KEY` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:109:| `SUPABASE_SERVICE_ROLE_KEY` | Backend (scripts only) | Ops smoke tests |` | Historical handoff, archived material, or generated artifact retained for reference only. |
| `SUPABASE_SERVICE_ROLE_KEY` | `docs/superpowers/plans/2026-04-02-supabase-connection-audit-final.md:138:- **Intentionally dormant:** `SUPABASE_SERVICE_ROLE_KEY` in ops scripts (tooling-only, not runtime)` | Historical handoff, archived material, or generated artifact retained for reference only. |

## Remaining Intentional Exceptions

- Backend compatibility fallbacks: `TRR-Backend/trr_backend/db/connection.py`, `TRR-Backend/scripts/_db_url.py`, and `TRR-Backend/scripts/db/run_sql.sh` may still read `SUPABASE_DB_URL` / `DATABASE_URL` as deprecated fallback inputs while runtime preference stays on `TRR_DB_URL` / `TRR_DB_FALLBACK_URL`.
- Vercel integration-managed retained envs: `docs/workspace/vercel-env-review.md` documents the reviewed retained `DATABASE_URL`, `POSTGRES_*`, and `SUPABASE_*` integration-managed values for the active `trr-app` project.
- Tooling-only DATABASE_URL consumers: `TRR-APP/apps/web/scripts/run-migrations.mjs` and related one-off scripts still require `DATABASE_URL`; operators should source it from `TRR_DB_URL` at invocation time instead of treating it as a runtime contract.
