# API Migration Ledger

This ledger maps TRR-APP direct SQL callers to backend API routes as direct SQL
is moved behind TRR-Backend.

Use [Backend Runtime Ownership](backend-runtime-ownership.md) to decide whether
a caller should move behind FastAPI or remain app-local in Next.js. In general,
backend-shared schema access moves to FastAPI; app-local authoring/editor access
can remain in Next.js until it becomes shared backend behavior.

## Required Row Fields

| Field | Meaning |
|---|---|
| App caller | File and function that previously used direct SQL. |
| Backend route | TRR-Backend route that now owns the data access. |
| App client | TRR-APP proxy/repository client that calls the backend route. |
| Auth context | Firebase/admin/internal context preserved by the migration. |
| Cacheability | Route cache, in-flight dedupe, and invalidation behavior. |
| Error contract | User/operator-visible error shape. |
| Validation | Focused backend/app tests or manual route proof. |

## Migrations

| App caller | Backend route | App client | Auth context | Cacheability | Error contract | Validation |
|---|---|---|---|---|---|---|
| `TRR-APP/apps/web/src/app/api/admin/trr-api/social/profiles/[platform]/[handle]/completion-summary/route.ts:GET` | `GET /api/v1/admin/socials/profiles/{platform}/{account_handle}/completion-summary` | Next route proxies with `fetchSocialBackendJson`, preserves normalized platform/handle/year, and attaches admin route timing | Firebase admin session is verified in TRR-APP and forwarded through `toVerifiedAdminContext`; backend enforces `InternalAdminUser` | Existing user-scoped five-minute cache, in-flight dedupe, cache hit/miss headers, and ten-minute stale fallback remain; backend miss is bounded by `TRR_ADMIN_SOCIAL_PROFILE_COMPLETION_SUMMARY_BACKEND_TIMEOUT_MS` (default 30 seconds) | Unsupported profiles remain `400 { error: "unsupported_profile" }`; stale data is served on upstream timeout; otherwise shared proxy/backend problem envelopes are preserved | `pytest -q tests/repositories/test_social_completion_summary.py tests/api/test_admin_social_completion_summary.py`; `pnpm exec vitest run -c vitest.config.mts tests/social-account-profile-completion-summary-route.test.ts --reporter=dot`; `python3 scripts/app-direct-sql-inventory.py --check` |
| `TRR-APP/apps/web/src/lib/server/admin/social-landing-repository.ts:safeLoadSocialProgressSummaries/safeLoadScrapeJobHealth` | `POST /api/v1/admin/socials/landing-progress-rollup`; `GET /api/v1/admin/socials/landing-scrape-job-health` | Social landing repository always uses `fetchSocialBackendJson`; the former no-admin progress SQL fallback and scrape-health SQL are removed | Verified admin context is forwarded when present; internal backend credentials cover server-only unauthenticated render paths; backend enforces the existing admin trust boundary | Backend rollup cache/degraded status remains authoritative; scrape-job health still degrades to the existing empty summary and failed progress reads remain uncacheable/stale | Progress failures preserve the existing unavailable/degraded status; scrape-health failures preserve the empty badge summary without breaking the landing payload | `pytest -q tests/repositories/test_social_completion_summary.py tests/api/test_admin_social_completion_summary.py`; `pnpm exec vitest run -c vitest.config.mts tests/social-landing-repository.test.ts --reporter=dot`; `python3 scripts/app-direct-sql-inventory.py --check` |
| `TRR-APP/apps/web/src/lib/server/trr-api/trr-shows-repository.ts:syncPersonExternalIds` | `PUT /api/v1/admin/people/{person_id}/external-ids` | `syncPersonExternalIds` now proxies with `fetchAdminBackendJson("/admin/people/{person_id}/external-ids", method=PUT)`; callers in the person external-ID route and social landing route stay unchanged at the browser/API boundary | Firebase admin session is verified in TRR-APP and forwarded as internal-admin JWT context when available; backend enforces `InternalAdminUser` | Backend invalidates person read cache after successful write; app still clears user-scoped person detail cache and calls `/admin/people/{person_id}/cache/invalidate` for compatibility | Unsupported sources return `400`, missing people return `404`, unique external-ID conflicts return `409`, database saturation returns the backend problem detail | `pytest tests/api/test_admin_person_external_ids.py tests/repositories/test_person_external_ids.py`; `pnpm exec vitest run tests/person-external-ids-route.test.ts tests/person-external-ids.test.ts --reporter=dot`; `python3 scripts/app-direct-sql-inventory.py --check` |
| `TRR-APP/apps/web/src/app/api/admin/trr-api/media-links/[linkId]/tags/route.ts`; removed tag-only helpers from `TRR-APP/apps/web/src/lib/server/trr-api/media-links-repository.ts` | `PUT /api/v1/admin/media-links/{link_id}/tags` | App route now proxies with `fetchAdminBackendJson("/admin/media-links/{link_id}/tags", method=PUT)` and preserves the browser-facing response payload | Firebase admin session is verified in TRR-APP and forwarded as internal-admin JWT context; backend enforces `InternalAdminUser` | No app cache; backend performs the media-link transaction that updates the target link and matching person gallery links | Missing links return `404`, database saturation returns backend problem detail, and other backend failures surface through `AdminReadProxyError` status mapping | `pytest tests/api/test_admin_media_links.py tests/repositories/test_media_link_tags.py`; `pnpm exec vitest run -c vitest.config.mts tests/tags-people-count-source-route.test.ts --reporter=dot`; `python3 scripts/app-direct-sql-inventory.py --check` |
| `TRR-APP/apps/web/src/lib/server/admin/social-posts-repository.ts` | `/api/v1/admin/shows/{show_id}/social-posts`, `/api/v1/admin/social-posts/{post_id}` | App social-post API routes via `fetchAdminBackendJson` / backend proxy | Firebase admin UID forwarded as `X-TRR-Admin-User-Uid` for writes; internal admin bearer for backend auth | No app direct-SQL cache; backend owns DB access | Backend 4xx/5xx mapped by app routes; route tests cover validation/error mapping | `pytest tests/api/test_admin_social_posts.py`; `pnpm exec vitest run -c vitest.config.ts tests/social-posts-show-route.test.ts tests/social-posts-postid-route.test.ts tests/social-posts-direct-sql-guard.test.ts --reporter=dot` |
| `TRR-APP/apps/web/src/lib/server/admin/cast-photo-tags-repository.ts:getTagsByPhotoIds` | `GET /api/v1/admin/cast-photos/tags` | `fetchAdminBackendJson("/admin/cast-photos/tags")` | Internal admin bearer via app backend proxy helper | Short app proxy timeout; backend helper returns empty array when tag table is unavailable | App degrades to empty `Map` on backend failure or non-200 status | `pytest tests/api/routers/test_admin_cast_photos.py`; `pnpm exec vitest run -c vitest.config.ts tests/cast-photo-people-count-route.test.ts tests/tags-people-count-source-route.test.ts --reporter=dot` |
| `TRR-APP/apps/web/src/lib/server/admin/cast-photo-tags-repository.ts:getPhotoIdsByPersonId` | `GET /api/v1/admin/cast-photos/tags/photo-ids` | `fetchAdminBackendJson("/admin/cast-photos/tags/photo-ids")` | Internal admin bearer via app backend proxy helper | Short app proxy timeout | App degrades to empty array on backend failure or non-200 status | same cast-photo validation slice |
| `TRR-APP/apps/web/src/lib/server/admin/cast-photo-tags-repository.ts:upsertCastPhotoTags` | `POST /api/v1/admin/cast-photos/tags` | `fetchAdminBackendJson("/admin/cast-photos/tags", method=POST)` | Internal admin bearer via app backend proxy helper; creator/updater UID fields preserved in payload | No app cache | App returns `null` on backend failure or non-200 status, preserving previous unavailable-table behavior | same cast-photo validation slice |
| `TRR-APP/apps/web/src/lib/server/admin/cast-photo-tags-repository.ts:setCastPhotoFaceBoxes` | `POST /api/v1/admin/cast-photos/{photo_id}/face-boxes` | `fetchAdminBackendJson("/admin/cast-photos/{photo_id}/face-boxes", method=POST)` | Internal admin bearer via app backend proxy helper | No app cache | App returns `false` on backend failure or non-200 status | same cast-photo validation slice |
| `TRR-APP/apps/web/src/lib/server/admin/reddit-sources-repository.ts:listRedditCommunities/listRedditCommunitiesWithThreads/getRedditCommunityById` | `GET /api/v1/admin/reddit/communities`, `GET /api/v1/admin/reddit/communities/{community_id}` | App reddit community routes via `loadStableRedditRead`; repository fallback/proxy via `fetchAdminBackendJson("/admin/reddit/communities*")` | Firebase admin session at app route; internal admin bearer via app backend proxy helper for backend reads | User-scoped stable list/detail cache with in-flight dedupe; backend read cache keyed by query/detail | Bad UUIDs return `400`, auth returns `401`/`403`, missing detail returns `404`, backend failures surface as `{ error }` | `pytest tests/api/test_admin_reddit_reads.py`; `pnpm exec vitest run -c vitest.config.ts tests/reddit-communities-route.test.ts tests/reddit-community-route.test.ts tests/reddit-sources-repository-backend.test.ts --reporter=dot` |
| `TRR-APP/apps/web/src/lib/server/admin/reddit-sources-repository.ts:createRedditCommunity/updateRedditCommunity/deleteRedditCommunity/updateRedditCommunityPostFlairs` | `POST /api/v1/admin/reddit/communities`, `PATCH /api/v1/admin/reddit/communities/{community_id}`, `DELETE /api/v1/admin/reddit/communities/{community_id}`, `PATCH/POST /api/v1/admin/reddit/communities/{community_id}/post-flairs` | App reddit community routes call repository functions, which proxy with `fetchAdminBackendJson` | Firebase admin UID forwarded as `X-TRR-Admin-User-Uid`; internal admin bearer for backend auth | Writes clear app stable list/detail/summary caches and backend reddit read cache | Validation preserves `400` messages, duplicate communities map to `409`, missing communities map to `404`, success returns `{ community }` or `{ success: true }` | `pytest tests/api/test_admin_reddit_reads.py`; `pnpm exec vitest run -c vitest.config.ts tests/reddit-communities-route.test.ts tests/reddit-community-route.test.ts tests/reddit-sources-repository-backend.test.ts --reporter=dot` |
| `TRR-APP/apps/web/src/lib/server/admin/reddit-sources-repository.ts:listRedditThreads/getRedditThreadById` | `GET /api/v1/admin/reddit/threads`, `GET /api/v1/admin/reddit/threads/{thread_id}` | App reddit thread routes via `loadStableRedditRead`; repository fallback/proxy via `fetchAdminBackendJson("/admin/reddit/threads*")` | Firebase admin session at app route; internal admin bearer via app backend proxy helper for backend reads | User-scoped stable list/detail cache with in-flight dedupe; backend read cache keyed by query/detail | Bad UUIDs return `400`, auth returns `401`/`403`, missing detail returns `404`, backend failures surface as `{ error }` | `pytest tests/api/test_admin_reddit_reads.py`; `pnpm exec vitest run -c vitest.config.ts tests/reddit-threads-route.test.ts tests/reddit-thread-route.test.ts tests/reddit-sources-repository-backend.test.ts --reporter=dot` |
| `TRR-APP/apps/web/src/lib/server/admin/reddit-sources-repository.ts:createRedditThread/updateRedditThread/deleteRedditThread` | `POST /api/v1/admin/reddit/threads`, `PATCH /api/v1/admin/reddit/threads/{thread_id}`, `DELETE /api/v1/admin/reddit/threads/{thread_id}` | App reddit thread routes call repository functions, which proxy with `fetchAdminBackendJson` | Firebase admin UID forwarded as `X-TRR-Admin-User-Uid`; internal admin bearer for backend auth | Writes clear app stable list/detail/summary caches and backend reddit read cache | Validation preserves `400` messages for UUID/Reddit URL/show-season checks, duplicate thread create maps to `409`, missing threads map to `404`, success returns `{ thread }` or `{ success: true }` | `pytest tests/api/test_admin_reddit_reads.py`; `pnpm exec vitest run -c vitest.config.ts tests/reddit-threads-route.test.ts tests/reddit-thread-route.test.ts tests/reddit-sources-repository-backend.test.ts --reporter=dot` |
| `TRR-APP/apps/web/src/lib/server/admin/covered-shows-repository.ts:getCoveredShows/isShowCovered/getCoveredShowIds/addCoveredShow/removeCoveredShow` | `GET/POST /api/v2/admin/covered-shows`; `GET/DELETE /api/v2/admin/covered-shows/{show_id}` | Existing Next endpoints `/api/admin/covered-shows` and `/api/admin/covered-shows/[showId]` call `fetchAdminBackendJson` with explicit `apiVersion: "v2"`; the legacy repository now contains only strict response parsing plus the backend-only social-landing fallback | Next verifies the Firebase admin session and signs `admin_uid` through `toVerifiedAdminContext`; backend enforces `InternalAdminUser` and ignores raw actor headers | Existing 30-second per-user list cache, cold-miss singleflight, and app invalidation remain; v1 and v2 share the backend service cache/invalidation while v1 stays available for rollback | Backend validation/not-found/capacity failures use stable problem details; Next preserves typed `502` unreachable/invalid-response, `503` unavailable/not-configured, and `504` timeout responses with no local SQL fallback | `pytest -q tests/api/test_admin_covered_shows_reads.py tests/api/routers/test_v2_admin_covered_shows.py`; `pnpm exec vitest run -c vitest.config.mts tests/covered-show-route-parity.test.ts tests/covered-shows-route-cache-dedupe.test.ts tests/covered-shows-route-metadata.test.ts tests/covered-shows-repository-slug-query-safety.test.ts --reporter=dot`; `python3 scripts/app-direct-sql-inventory.py --check --fail-expired --as-of 2026-07-16` |
| `TRR-APP/apps/web/src/app/api/admin/recent-people/route.ts:GET/POST`; deleted `TRR-APP/apps/web/src/lib/server/admin/recent-people-repository.ts` | `GET /api/v2/admin/recent-people`; `POST /api/v2/admin/recent-people` | Existing Next endpoint `/api/admin/recent-people` now proxies directly with `fetchAdminBackendJson` using explicit `apiVersion: "v2"` and strict payload parsing; the old app SQL repository is removed instead of wrapped | Next verifies the Firebase admin session and signs `admin_uid` through `toVerifiedAdminContext`; backend enforces `InternalAdminUser` and ignores raw actor headers | Existing user-scoped cache, cold-miss singleflight, and post-write invalidation remain unchanged; v1 rollback route remains available on the backend | Backend validation/capacity failures use stable problem details; Next preserves typed proxy errors and rejects malformed backend payloads with `502 INVALID_BACKEND_RESPONSE`, with no local SQL fallback | `pytest -q tests/api/test_admin_recent_people.py tests/api/routers/test_v2_admin_recent_people.py tests/repositories/test_recent_people_repository.py`; `pnpm exec vitest run -c vitest.config.mts tests/recent-people-route.test.ts tests/admin-api-references-generator.test.ts --reporter=dot`; `python3 scripts/app-direct-sql-inventory.py --check --fail-expired --as-of 2026-07-16` |
| Deleted `trr-shows-repository.ts:listShowExternalIdsByIds/listPersonExternalIds/listPrimaryPersonExternalIdsByPersonIds`; live callers remain the person external-ID route and social landing route/repository | `GET /api/v2/admin/people/{person_id}/external-ids`; `POST /api/v2/admin/people/external-ids/batch`; `POST /api/v2/admin/shows/external-ids/batch` | `admin-external-id-reads.ts` provides strict response parsing, max-200 request chunking, first-seen map order, and the prior empty/missing map behavior; existing Next payloads remain unchanged | Next verifies the Firebase admin session and forwards `toVerifiedAdminContext`; every backend route enforces `InternalAdminUser` | Person detail remains uncached; social landing retains its existing route cache and enrichment failure fallbacks; writes retain their existing app/backend invalidation flow | Invalid UUID/query/body inputs return stable problem `400`, missing single-person reads return `404`, database capacity returns safe `503`, and app response drift returns typed `502 INVALID_BACKEND_RESPONSE` | `pytest -q tests/api/routers/test_v2_admin_external_ids.py tests/repositories/test_external_id_reads.py`; `pnpm exec vitest run tests/admin-external-id-reads.test.ts tests/person-external-ids-route.test.ts tests/social-landing-repository.test.ts tests/social-landing-route-cache.test.ts`; `python3 scripts/app-direct-sql-inventory.py --check --fail-expired --as-of 2026-07-16` |
| Deleted production-dead `trr-shows-repository.ts:resolvePersonSlug/getShowByImdbId` and their obsolete repository tests | No new route: public person identity is already owned by `GET /api/v2/identities/people/{slug}`, while the IMDb lookup had no production caller | No app client change; live public pages already use `public-identities.ts`, and no production import referenced either deleted repository API | Not applicable because no live request path changed | Not applicable; no live cache path referenced either function | No user-visible error contract changed; the active public identity adapter retains its strict v2 problem handling | `pnpm exec vitest run -c vitest.config.mts tests/trr-shows-repository-resolve-slug.test.ts tests/trr-shows-repository-slug-query-safety.test.ts tests/show-route-parity.test.ts`; `python3 -m pytest -q scripts/test_app_direct_sql_inventory.py`; `python3 scripts/app-direct-sql-inventory.py --check --fail-expired --as-of 2026-07-16` |
| `resolveAdminShowId`, admin show resolve-slug, and show-detail slug fallback; deleted app-local SQL `trr-shows-repository.ts:resolveShowSlug` | `GET /api/v2/identities/shows/{slug}` with `GET /api/v1/admin/trr-api/shows/resolve-slug` retained as the N/N+1 rollback route | `public-identities.ts:resolveShowSlug` normalizes legacy eight-character collision suffixes, maps strict public identity responses into the existing admin shape, and validates the legacy rollback envelope | Public v2 identity reads are anonymous/read-only; the bounded v1 rollback call uses the existing internal-admin backend proxy contract | Public identity fetches retain React request dedupe; the admin resolve route retains its user-scoped response cache and in-flight dedupe | Invalid or ambiguous v2 aliases return the prior unresolved/null behavior; v2 404/unavailable states try v1, legacy 404 stays unresolved, and malformed payloads become typed `502 INVALID_BACKEND_RESPONSE` | `pytest -q tests/migrations/test_public_identity_slug_aliases.py tests/repositories/test_public_identities.py tests/services/test_public_identity.py tests/api/routers/test_v2_public_identities.py`; `pnpm exec vitest run -c vitest.config.mts tests/trr-shows-repository-resolve-slug.test.ts tests/public-identity-contract.test.ts tests/show-route-parity.test.ts tests/show-featured-image-validation-route.test.ts`; `python3 scripts/app-direct-sql-inventory.py --check --fail-expired --as-of 2026-07-16` |
| Show nickname collision guard in `TRR-APP/apps/web/src/app/api/admin/trr-api/shows/[showId]/route.ts`; deleted direct SQL `trr-shows-repository.ts:getShowByExactSlug` | `GET /api/v2/admin/shows/exact-slug/{slug}` with legacy `GET /api/v1/admin/trr-api/shows/resolve-slug` plus `GET /api/v1/admin/trr-api/shows/{show_id}` retained as the bounded N/N+1 rollback; `20260716050000_core_show_slug_normalized_unique.sql` enforces unique `lower(btrim(slug))` ownership without rewriting show rows | `admin-show-slug-reads.ts:getAdminShowByExactSlug` normalizes the requested slug, strictly validates the v2 envelope, and accepts a legacy resolution only when the detail row's stored slug is an exact match | Next verifies the Firebase admin session and forwards `toVerifiedAdminContext`; backend enforces `InternalAdminUser` and rejects unsigned raw admin headers | The v2 exact lookup is intentionally uncached and performs one repository query per request using the normalized unique-index expression and predicate; rollback retains the existing v1 caches and runs only for the exact missing-route or availability allowlist | `SHOW_NOT_FOUND` is terminal `404`; auth, malformed payloads, slug mismatches, and structured `500` failures do not roll back; only exact route absence, `405/502/503/504`, or named transport/configuration errors can use v1; alias-only matches return no collision | `pytest -q tests/migrations/test_core_show_slug_normalized_unique.py tests/repositories/test_show_slug_reads.py tests/api/routers/test_v2_admin_show_slugs.py tests/api/test_internal_admin_openapi.py`; `pnpm exec vitest run tests/admin-show-slug-reads.test.ts tests/show-featured-image-validation-route.test.ts tests/show-route-parity.test.ts tests/trr-shows-repository-slug-query-safety.test.ts`; `python3 -m pytest -q scripts/test_app_direct_sql_inventory.py`; `python3 scripts/app-direct-sql-inventory.py --check --fail-expired --as-of 2026-07-16`; `make openapi-v2-contract-check` |
| `TRR-APP/apps/web/src/lib/server/admin/networks-streaming-repository.ts:getNetworksStreamingSummary`; summary consumers in the admin networks page and brand profile repository | `GET /api/v2/admin/networks-streaming/summary`; v1 `GET /api/v1/admin/shows/networks-streaming/summary` delegates to the same backend service for rollback | `admin-networks-streaming-reads.ts:getNetworksStreamingSummary` validates the strict v2 response and preserves the existing browser-facing summary payload; both Next routes pass the verified admin context | Next verifies the Firebase admin session and signs `admin_uid` through `toVerifiedAdminContext`; backend enforces `InternalAdminUser` | The existing per-user app cache and singleflight remain; v1 and v2 share the backend 15-second cache, singleflight, and invalidation path | Malformed success payloads and structured backend/auth failures are terminal; rollback is limited to exact route absence, `405`, unstructured gateway `502/503/504`, or explicit transport/configuration availability errors | `pytest -q tests/services/test_networks_streaming_reads.py tests/api/routers/test_v2_admin_networks_streaming.py`; `pnpm exec vitest run -c vitest.config.mts tests/admin-networks-streaming-reads.test.ts tests/networks-streaming-summary-route.test.ts tests/networks-streaming-summary-route-cache-dedupe.test.ts tests/brand-profile-repository.test.ts tests/brand-profile-route.test.ts`; `python3 -m pytest -q scripts/test_app_direct_sql_inventory.py`; `python3 scripts/app-direct-sql-inventory.py --check --fail-expired --as-of 2026-07-16`; `make openapi-v2-contract-check` |
| Deleted `TRR-APP/apps/web/src/lib/server/admin/networks-streaming-repository.ts:getNetworkStreamingDetail/getNetworkStreamingSuggestions/tableExists`; brand profiles now consume backend-owned detail and family context | `GET /api/v2/admin/networks-streaming/detail`; v1 `GET /api/v1/admin/shows/networks-streaming/detail` delegates to the same service for rollback | `admin-networks-streaming-reads.ts:getNetworkStreamingDetail` normalizes lookup keys, strictly validates the complete nested detail/family/logo/link/Wikipedia payload through `admin-networks-streaming-contracts.ts`, and removes the separate brand-family request | Next passes the verified Firebase admin context to both v2 and bounded v1 calls; both backend routes enforce `InternalAdminUser` | v1 and v2 share the backend 30-second cache, singleflight, family enrichment, and generation-based invalidation; an invalidated in-flight load cannot repopulate stale data | Typed `NETWORKS_STREAMING_ENTITY_NOT_FOUND` 404 responses carry strictly parsed suggestions and never roll back; malformed 200/404 payloads, auth, and structured failures are terminal; only exact route absence or the availability allowlist uses v1 | `pytest -q tests/services/test_networks_streaming_reads.py tests/services/test_covered_shows.py tests/repositories/test_admin_networks_streaming_reads_repository.py tests/api/test_admin_networks_streaming_reads.py tests/api/routers/test_v2_admin_networks_streaming.py`; `pnpm exec vitest run -c vitest.config.mts tests/admin-networks-streaming-reads.test.ts tests/networks-streaming-detail-route.test.ts tests/brand-profile-repository.test.ts tests/brand-profile-route.test.ts`; `python3 -m pytest -q scripts/test_app_direct_sql_inventory.py`; `python3 scripts/app-direct-sql-inventory.py --check --fail-expired --as-of 2026-07-16`; `make openapi-v2-contract-check` |
| `person-cover-photos-repository.ts:getCoverPhoto/getCoverPhotos/setCoverPhoto/removeCoverPhoto`; `person-thumbnail-crops-repository.ts:updateCastPhotoThumbnailCrop/updateMediaLinkThumbnailCrop` | `GET/PUT/DELETE /api/v2/admin/people/{person_id}/cover-photos`; `PUT /api/v2/admin/people/{person_id}/thumbnail-crops` | The existing repositories are now strict backend adapters; the cover-photo browser route preserves its `{ coverPhoto }` shape, and the thumbnail-crop route preserves its flat crop result and missing-photo `404`. Cover reads use v1 `GET /api/v1/admin/people/{person_id}/cover-photo` only when v2 returns the exact framework-level missing-route response | Every v2 call receives `toVerifiedAdminContext(user)` and backend routes enforce `InternalAdminUser`; cover writes derive `created_by_firebase_uid` from the signed admin identity and ignore raw actor headers | Existing user-scoped cover-photo cache and person gallery invalidation remain; writes still invalidate the legacy backend person-read cache for v1 consumers. There is no app SQL or write fallback | Strict app parsing rejects extra or malformed fields, mismatched person/photo IDs, invalid URLs/timestamps/crop bounds, and partial crop tuples with typed `502 INVALID_BACKEND_RESPONSE`; crop not-found remains `404 { error: "Photo not found" }`; database capacity uses the safe v2 `503` problem | `pytest -q tests/repositories/test_person_media_admin.py tests/api/routers/test_v2_admin_person_media.py tests/api/test_internal_admin_openapi.py`; `pnpm exec vitest run tests/person-cover-photos-repository.test.ts tests/person-thumbnail-crops-repository.test.ts tests/person-cover-photo-route.test.ts tests/person-thumbnail-crop-route.test.ts`; `pnpm run typecheck`; targeted ESLint; `python3 -m pytest -q scripts/test_app_direct_sql_inventory.py`; `python3 scripts/app-direct-sql-inventory.py --check --fail-expired --as-of 2026-07-16`; `make openapi-v2-contract-check` |
| `season-cast-survey-roles-repository.ts:listSeasonCastSurveyRoles/upsertSeasonCastSurveyRole/deleteSeasonCastSurveyRole/replaceSeasonCastSurveyRoles`; survey detail, survey-cast, and survey-creation consumers | `GET/POST/PATCH/DELETE /api/v2/admin/shows/{show_id}/seasons/{season_number}/cast-survey-roles` | The repository is now a strict backend adapter and retains its existing exported row/types and function behavior; the show/season key intentionally preserves survey-role writes before a matching `core.seasons` row exists | Every caller derives and forwards `toVerifiedAdminContext(user)`; backend routes enforce `InternalAdminUser`; raw caller headers are not an authorization path | The existing user-scoped survey-detail and survey-season-cast caches, cold-miss singleflight, refresh semantics, and post-write invalidation stay in the Next routes; the backend persistence methods are uncached | Browser validation, payloads, and broad route status mapping remain unchanged; backend validation/capacity failures use stable problem details; app parsing rejects extra fields, malformed UUID/timestamps, mismatched scope, and write-response drift with `502 INVALID_BACKEND_RESPONSE`; there is no app SQL fallback | `pytest -q tests/repositories/test_season_cast_survey_roles.py tests/api/routers/test_v2_admin_season_cast_survey_roles.py tests/api/test_internal_admin_openapi.py`; `pnpm exec vitest run -c vitest.config.mts tests/season-cast-survey-roles-repository.test.ts tests/season-cast-survey-route.test.ts tests/survey-detail-route.test.ts tests/survey-from-show-route.test.ts`; `pnpm run typecheck`; targeted ESLint; `python3 -m pytest -q scripts/test_app_direct_sql_inventory.py`; `python3 scripts/app-direct-sql-inventory.py --check --fail-expired --as-of 2026-07-16`; `make openapi-v2-contract-check` |
| `trr-shows-repository.ts:searchShows/getShowById/getSeasonsByShowId/getSeasonById/getSeasonByShowAndNumber/getEpisodesBySeasonId/getEpisodesByShowAndSeason/getEpisodeById/searchEpisodes` (10 retired SQL rows because `getSeasonsByShowId` previously issued two queries) | `GET /api/v2/shows`; `GET /api/v2/shows/{show_id}`; `GET /api/v2/shows/{show_id}/seasons`; `GET /api/v2/seasons/{season_id}`; `GET /api/v2/shows/{show_id}/seasons/{season_number}`; `GET /api/v2/seasons/{season_id}/episodes`; `GET /api/v2/shows/{show_id}/seasons/{season_number}/episodes`; `GET /api/v2/episodes`; `GET /api/v2/episodes/{episode_id}` | The existing exported repository functions now call `fetchAdminBackendJson` with `apiVersion: "v2"`, preserve pagination/search inputs, and map the v2 show/season/episode envelopes back to the existing app types; affected page and API consumers keep their imports unchanged | These endpoints are anonymous public read-only routes; no admin identity or raw actor header is accepted or required | The backend repository performs bounded paginated reads without a new cache; existing consumer-level React/route caching remains unchanged | List `404` responses preserve the existing empty-list behavior, detail `404` responses preserve `null`, invalid pagination/search/UUID inputs return stable v2 problems, database saturation returns retryable `503`, and other upstream failures use the existing typed admin-backend status error | `pytest -q tests/repositories/test_core_show_reads.py tests/services/test_core_show_reads_service.py tests/api/routers/test_v2_core_show_reads.py tests/api/test_internal_admin_openapi.py`; `pnpm exec vitest run -c vitest.config.mts tests/trr-shows-repository-slug-query-safety.test.ts tests/show-route-parity.test.ts tests/survey-detail-route.test.ts tests/reddit-community-episode-refresh-route.test.ts --reporter=dot`; `/opt/homebrew/bin/ruff check` on the focused backend slice; `python3 -m pytest -q scripts/test_app_direct_sql_inventory.py`; `python3 scripts/app-direct-sql-inventory.py --check --fail-expired --as-of 2026-07-16`; `make openapi-v2-contract-check` |
| `trr-shows-repository.ts:` (27 retired SQL rows) | `GET /api/v2/shows/{show_id}/cast`; `GET /api/v2/seasons/{season_id}/cast`; `GET /api/v2/people/{person_id}/credits`; `GET /api/v2/people/{person_id}/episode-credits` | Existing repository exports now use the generated v2 schema types and preserve the established show-cast views, season episode counts, pagination, archive filters, curated-show set, and person/show credit helper behavior; callers keep their imports unchanged | These endpoints are anonymous public read-only routes; no admin identity or raw actor header is accepted or required | Backend reads are bounded at 500 rows; local photo precedence remains authoritative and the opt-in Bravo fallback is bounded, concurrent, timed out, and soft-failing; existing consumer route/React caches remain unchanged | Valid missing IDs return empty arrays, invalid UUID/query inputs return stable `400` problems, database saturation returns retryable `503`, and app adapters preserve empty-list `404` behavior while surfacing other typed upstream failures | From `TRR-Backend`: `.venv/bin/python -m pytest -q tests/repositories/test_core_cast_credit_reads.py tests/services/test_core_cast_credit_reads_service.py tests/integrations/imdb/test_name_filmography.py tests/api/routers/test_v2_core_cast_credit_reads.py tests/api/routers/test_v2_core_show_reads.py`; from `TRR-APP/apps/web`: `pnpm exec vitest run tests/trr-shows-repository-core-cast-credit-adapter.test.ts tests/trr-shows-repository-photo-fallback.test.ts tests/trr-shows-repository-person-episode-credits.test.ts tests/trr-shows-repository-person-show-scope-credits.test.ts`; `/opt/homebrew/bin/ruff check` on the focused backend slice; `python3 -m pytest -q scripts/test_app_direct_sql_inventory.py`; `python3 scripts/app-direct-sql-inventory.py --check --fail-expired --as-of 2026-07-16`; `make openapi-v2-contract-check` |
| `trr-shows-repository.ts:getPersonById/searchPeople/getDeducedFamilyRelationshipsByPersonId`; deleted zero-caller `searchPeopleWithShowContext/getPeopleMostShows/getPeopleTopEpisodes/getPeopleRecentlyAdded/getPeopleMostPopular` (11 retired SQL rows) | `GET /api/v2/admin/people`; `GET /api/v2/admin/people/{person_id}`; `GET /api/v2/admin/people/{person_id}/relationships` | Existing person detail/search helpers now use generated v2 person schemas and the strict backend adapter; the admin people search route forwards verified admin context, while the five unused ranking/search exports were removed rather than migrated | Next verifies the Firebase admin session and signs `admin_uid` through `toVerifiedAdminContext`; backend enforces `InternalAdminUser`, and internal server-only callers retain the signed service credential path | Reads are bounded at 500 rows and intentionally uncached; the existing browser/route behavior and prefix-search ordering remain unchanged | Detail `404` remains `null`; relationship reads preserve the legacy empty-map result when no assignments exist; invalid UUID/query/pagination inputs return stable `400` problems, database saturation returns retryable `503`, and other failures use the typed admin-backend status error | From `TRR-Backend`: `.venv/bin/python -m pytest -q tests/repositories/test_core_people_reads.py tests/services/test_core_people_reads_service.py tests/api/routers/test_v2_admin_people_reads.py tests/api/test_internal_admin_openapi.py`; from `TRR-APP/apps/web`: `pnpm exec vitest run tests/trr-shows-repository-core-people-adapter.test.ts tests/admin-people-route.test.ts`; `pnpm run typecheck`; targeted ESLint and Ruff; `python3 -m pytest -q scripts/test_app_direct_sql_inventory.py`; `python3 scripts/app-direct-sql-inventory.py --check --fail-expired --as-of 2026-07-16`; `make openapi-v2-contract-check`; `make architecture-hotspots-check` |
| `trr-shows-repository.ts:validateShowImageForField/getAssetsByShowSeason`; `images-repository.ts`; `media-links-repository.ts`; media-link asset preflight; deleted zero-caller `getPhotosByPersonId/getAssetsByShowId/getFandomDataByPersonId` (35 retired SQL rows) | `GET /api/v2/admin/shows/{show_id}/seasons/{season_number}/assets`; `POST /api/v2/admin/shows/{show_id}/featured-image-validation`; `GET/DELETE/PUT /api/v2/admin/images/{image_type}/{image_id}*`; `GET/POST/PATCH /api/v2/admin/media-links*` | Existing repository exports are now strict v2 adapters, affected Next routes preserve their browser payloads, the survey consumer still receives a plain assets array, and production-dead photo/show-asset/fandom implementations were removed while their shared exported types remain | Every affected Next route verifies the Firebase admin session and forwards `toVerifiedAdminContext`; backend routes enforce `InternalAdminUser`, and image audit actors come only from the signed `admin_uid` | Reads remain bounded at 500 and existing route/consumer caching is unchanged; image/media mutations are backend-owned and retain best-effort audit behavior, including missing-audit-table tolerance | Invalid UUID/type/body inputs return stable problem `400`, missing image/media records use explicit `404` where the browser contract expects it, database capacity returns safe retryable `503`, app adapters preserve typed upstream failures, and explicit people-count zero survives metadata fallback | `pytest -q tests/repositories/test_admin_media_repository.py tests/repositories/test_admin_show_reads_people_count.py tests/repositories/test_admin_show_reads.py tests/services/test_admin_media_service.py tests/api/routers/test_v2_admin_media.py tests/api/test_internal_admin_openapi.py`; `pnpm exec vitest run tests/packet2d-admin-adapters.test.ts tests/packet2d-image-media-routes.test.ts tests/trr-shows-repository-featured-image-validation.test.ts tests/show-featured-image-validation-route.test.ts tests/survey-detail-route.test.ts tests/media-link-context-route.test.ts tests/trr-shows-repository-core-people-adapter.test.ts`; `pnpm exec tsc --noEmit`; targeted ESLint and Ruff; `python3 -m pytest -q scripts/test_app_direct_sql_inventory.py`; `python3 scripts/app-direct-sql-inventory.py --check --fail-expired --as-of 2026-07-17`; `make openapi-v2-contract-check`; `make architecture-hotspots-check` |

| `reddit-sources-repository.ts` post resolution/detail/window count/window list reads and their four authenticated Next handlers (15 retired governed SQL rows) | `GET /api/v2/admin/reddit/posts/resolve`; `GET /api/v2/admin/reddit/posts/{post_id}`; `GET /api/v2/admin/reddit/post-window-counts`; `GET /api/v2/admin/reddit/post-windows` | The existing repository and routes now use strict v2 backend adapters and preserve browser payloads, pagination, stable window keys, cached route behavior, and missing-post handling | Every Next route verifies Firebase admin and forwards `toVerifiedAdminContext`; backend routes enforce `InternalAdminUser` | Existing route caches and invalidation remain; there is no local SQL fallback | Stable v2 `400/404/500/503` problems are mapped to existing app responses and malformed success payloads fail closed | `pytest -q tests/api/routers/test_v2_admin_reddit_reads.py`; focused Reddit app tests (4 files, 16 tests); direct-SQL inventory check |
| Recent account-catalog run lookup in `.../catalog/runs/recent/route.ts` (1 retired governed SQL row) | `GET /api/v2/admin/social/profiles/{platform}/{handle}/catalog/runs/recent` | The Next route calls the strict v2 adapter while retaining its user-scoped fresh/stale cache and normalized run/follow-up payload | Firebase admin is verified and forwarded with `toVerifiedAdminContext`; backend enforces `InternalAdminUser` | Existing fresh/stale cache and in-flight behavior remain; no SQL fallback | Unsupported platform/handle inputs and backend failures use stable typed problems | `pytest -q tests/api/routers/test_v2_admin_social_catalog_runs.py`; `vitest ... social-account-catalog-recent-runs-route.test.ts`; direct-SQL inventory check |
| Shared social-account source reads/writes in `shared-account-sources.ts` and the social-landing fallback (1 retired governed SQL row) | `GET/PUT /api/v2/admin/socials/shared-account-sources` | `shared-account-sources.ts`, its Next route, and social landing now use the canonical v2 adapter; the local shared-schema fallback is removed | Verified Firebase admin context is forwarded when present; signed internal service context supports server-only landing assembly; backend enforces `InternalAdminUser` | Existing route/landing cache behavior remains and backend failures stay non-cacheable | Strict normalization and problem responses preserve source-scope/platform validation; unavailable backend no longer silently reads local SQL | Focused shared-source backend tests (107 passing with control-plane compatibility); shared-source and social-landing app suites; direct-SQL inventory check |
| Admin typography state, set CRUD, and assignment upsert in `typography-repository.ts` (13 retired governed SQL rows) | `GET /api/v2/admin/site-typography`; `POST /sets`; `PUT/DELETE /sets/{set_id}`; `PUT /assignments` | The admin repository and four admin Next handlers now proxy strict v2 contracts; the anonymous public Next route and its full persisted payload remain unchanged | Admin handlers verify Firebase admin and forward `toVerifiedAdminContext`; backend enforces `InternalAdminUser`; the public route remains anonymously readable through its existing server boundary | Existing admin/public route caches and invalidation remain; missing/empty backend tables return the seeded state without request-time DDL | Missing set remains `404`, assigned-set delete remains `409`, malformed backend data fails closed, and database capacity is a stable `503` | `pytest -q tests/repositories/test_admin_typography.py tests/api/routers/test_v2_admin_typography.py`; three app files / 14 tests; focused lint; direct-SQL inventory check |
| Flashback quiz/event administration in `flashback-admin-repository.ts` and four admin handlers (12 retired governed SQL rows) | `GET/POST/PATCH /api/v2/admin/flashback/quizzes*`; `GET/POST .../events`; `DELETE /api/v2/admin/flashback/events/{event_id}` | The repository and handlers now use strict v2 CRUD adapters while preserving quiz publication and ordered event behavior | Firebase admin is verified by the Next routes and forwarded through the existing admin proxy; backend enforces `InternalAdminUser` | No app SQL cache/fallback; backend owns the atomic create/delete-and-compact transactions | Validation preserves year/point rules, missing quiz/event `404`, delete `204`, and typed database failures | `pytest -q tests/repositories/test_admin_flashback.py tests/api/routers/test_v2_admin_flashback.py`; `vitest ... flashback-admin-routes.test.ts`; direct-SQL inventory check |
| `trr-shows-repository.ts:updateShowById/listEffectivePersonSocialHandlesByPersonIds/updatePersonCanonicalProfileSourceOrder` and exact show/person/social-landing consumers (3 retired governed SQL rows) | `PATCH /api/v2/admin/shows/{show_id}`; `POST /api/v2/admin/people/effective-social-handles`; `PATCH /api/v2/admin/people/{person_id}/canonical-profile-source-order` | Existing app repository exports and route payloads remain compatible while all three seams now call strict v2 endpoints; social landing also consumes the shared-source v2 adapter | All browser-facing handlers verify Firebase admin and forward `toVerifiedAdminContext`; backend enforces `InternalAdminUser` | Existing show/person/landing caches and invalidation remain; there is no local SQL fallback | UUID/body/source-order validation, slug collision, missing resources, and database capacity retain stable typed failures | `pytest -q tests/repositories/test_admin_show_person_writes.py tests/api/routers/test_v2_admin_show_person_writes.py`; five app files / 54 tests; focused Ruff/ESLint; direct-SQL inventory check |

## Unapplied Data Guards

| Migration | Purpose | Current read-only evidence | Validation | Promotion state |
|---|---|---|---|---|
| `TRR-Backend/supabase/migrations/20260716050000_core_show_slug_normalized_unique.sql` | Make case/whitespace-normalized show-slug ownership unique and align the exact-slug query with the same `lower(btrim(slug))` expression. The migration fails before index creation when duplicates exist and never rewrites or deletes show rows. | Repo-configured Supabase count query on 2026-07-16: 6 populated slugs, 0 non-normalized rows, 0 normalized duplicate groups. No row values were retrieved. | `pytest -q tests/migrations/test_core_show_slug_normalized_unique.py tests/repositories/test_show_slug_reads.py tests/api/routers/test_v2_admin_show_slugs.py`; `make migration-ownership-lint` | Authored only; not applied to preview or production. Requires the named Gate 4B data approval, pending-set proof, lock/timeout evidence, and post-apply index/checksum verification. |
| `TRR-Backend/supabase/migrations/20260716133000_admin_season_cast_survey_roles.sql` | Canonically promote the skipped TRR-APP `022_create_admin_season_cast_survey_roles.sql` contract into the backend chain without changing its composite show/season/person identity, roles, indexes, trigger, grants, or later RLS/default-deny posture. | Static reconciliation covers the app backlog DDL, backend RLS/function-hardening migrations, backend repository conflict key, guarded destructive rollback, and an aggregate/catalog-only verification script. No live schema or row query was performed in this packet. Current ordering avoids an out-of-order live apply; immutable April/June history still means fresh-zero backend-only replay needs a future approved baseline/squash. | `pytest -q tests/migrations/test_admin_season_cast_survey_roles.py tests/repositories/test_season_cast_survey_roles.py tests/api/routers/test_v2_admin_season_cast_survey_roles.py`; `make migration-ownership-lint`; `git diff --check` | Authored only; not applied to preview or production. Gate 4B requires pending-set/checksum proof; read-only column/constraint/index/trigger/RLS/policy/privilege snapshots; aggregate invalid/null/duplicate counts; preview apply under the bounded timeouts; explicit live approval; and post-apply migration/schema/endpoint verification. Do not backdate this migration or rewrite applied checksums. |
